# Checkpoints

## Product Definition

- [x] 已明确不是普通网页生图工具。
- [x] 已明确当前方向不是技术图示 DSL 优先。
- [x] 已明确能力平台定位：MCP/API/CLI + provider adapter + asset registry。
- [x] 已冻结输入入口：MCP、REST API、CLI、Web UI。
- [x] 已冻结输出：任务、资产身份、原图、缩略图、metadata、交付信息。
- [x] 已冻结业务隔离：Workspace、Project、Campaign。
- [x] 已选择核心业务流程：内容系统批量生成封面图。
- [x] 已冻结第一版架构方向：小核心、多入口、适配器和大存储。
- [x] 已合并架构评审，形成最终架构指导文件。
- [x] 已完成实施前业务流程模拟。
- [x] 已确认第一阶段 provider：mock provider first，后续云端 API provider。
- [x] 已确认第一阶段交付目标：本地 API 下载 URL、缩略图 URL 和 metadata URL。
- [x] 已确认最终自托管部署方式：Docker Compose。

## MVP Validation

- [x] 已导入可运行 Web 前端底座。
- [x] Web 支持 Base URL、API Key 和多 provider 配置。
- [x] 可创建结构化图片任务。
- [x] 可在内容账号 project 和 campaign 下创建图片任务。
- [x] 可生成或模拟生成图片。
- [x] 图片可保存到稳定路径。
- [x] 资产有 `asset_id` 和 metadata。
- [x] 资产有缩略图。
- [x] 资产可按 project / campaign 隔离查询。
- [x] 选优/状态标记可变更。
- [x] API/CLI/MCP 入口可跑通。
- [x] MCP stdio server 可列出 tools 并通过 tool call 创建任务、查询任务、标记 selected 和获取 delivery。
- [x] 服务端 Worker 已支持第一个真实 provider adapter：OpenAI-compatible。
- [x] Web 已支持服务端托管模式：创建服务端 ImageTask、轮询 assets、展示服务端候选图并执行 select/reject。
- [x] Prompt template、style preset、reference image 参数和 generation config 已形成服务端项目级复用策略。
- [x] 多候选 best-of 自动选优已形成第一版服务端策略：`selection_mode=auto` / `best_of` 时自动 selected 一张候选。
- [x] best-of 已支持可插拔 scorer：任务/quality profile 可指定 `best_of_config`，当前已接入 `local_metadata_v1` 与 `http_judge_v1`，外部 judge 失败时回退本地启发式。
- [x] best-of 已支持可选 auto reject：`best_of_config.auto_reject_non_selected=true` 时，未入选候选会自动 rejected，且后续仍可人工重新 select。
- [x] Web/MCP/REST 已能把 reference image、mask/edit descriptor 和更多 generation config 带入服务端任务，并写入 asset `parameters_json`。
- [x] Web/REST 已支持 scope 内 `input-files` 上传；OpenAI-compatible 任务在存在已解析输入文件时会走 `/images/edits` multipart，并完成服务端 asset 闭环。
- [x] REST/MCP 已支持 remote URL 抓取和当前项目 `asset_id` 复用；OpenAI-compatible 可把这些来源解析到 `resolved_input_files` 并继续完成 `/images/edits` 闭环。
- [x] 服务端 `provider=fal` 已支持 queue 文生图，以及基于 `resolved_input_files` 的 storage upload + edit 闭环，可复用 `input-files`、匿名 remote URL 和当前项目 `asset_id`。
- [x] 本地 repair/reconcile smoke 已接入：`vag repair scan`、`vag repair requeue`、`vag repair verify-asset` 可发现并修复入队失败任务、校验资产文件。
- [x] HTTP API 已支持基础实例级 / project 级限流：配置阈值后返回 `429`、结构化错误 JSON 和 `Retry-After`，MCP stdio / Worker 不受直接影响。
- [x] HTTP / API 已支持第一版结构化审计日志：`/api/*` 请求会写入本地 JSONL 审计事件，并可通过 `vag audit list` 按 project / task / status 过滤查询。
- [x] 项目级 access config 已支持多把命名 key：可 add/update/disable/delete 单把 key，任意启用 key 都可访问 project 级资源，审计会优先记录命中的 key 名称。
- [x] Web 已支持服务端资产库同步：当前 scope 下来自 MCP / REST / CLI / Web 的 assets 可通过服务端 list assets 接口显示，不依赖浏览器本地任务历史。
- [x] Web 已支持最小资产库视图：可显示缩略图、prompt、provider、model、status、source、task_id、asset_id、created_at、delivery links，并可 select/reject/copy/open。
- [x] Scope 管理已升级为基础控制台：workspace / project / campaign 列表显示资产数量、selected/rejected 数和最近活动，且可从控制台切换当前 scope。
- [x] Asset response 已暴露任务 `metadata_json`：MCP/REST 传入的 `source/session/batch/story/scene/target_path` 可在资产 metadata 和 Web 资产库中显示。
- [x] P1 Storage Governance 已接入：服务端可只读统计 instance/workspace/project/campaign 存储占用，REST 可返回当前 scope + 实例级 usage/counts，Web Scope 管理可展示 storage/original/thumbnail/metadata/input-files 等占用，CLI 可 dry-run 预览 rejected/generated/tmp/orphan 清理候选。
- [x] P1 Storage Governance 已完成受控执行与治理可见性：`vag storage cleanup-execute` 需要 `--execute` 加 dry-run token 或显式确认；selected/published/approved 默认 protected；执行会写本地 CLI audit；REST/Web 可读取脱敏 `storage-integrity` 摘要。
- [x] P1 Asset Production Readiness 已完成：REST/CLI 资产列表支持 limit/status/provider/model/source/session_id/batch_id/keyword/date 筛选；Web 资产库支持筛选、加载更多、lazy loading 和 metadata/parameters 摘要；`metadata_json` 标准化 source/session/run/batch/story/scene/target_path；项目级非敏感 provider profile 可保存并作为默认 provider/model 复用。
- [x] P1 Web Performance / Startup 已完成：Web 启动初始化已幂等；本地 thumbnail backfill、TaskGrid 渲染、服务端资产库保留节点、fal/custom 恢复轮询和 Scope 控制台统计都有边界；Agent/Settings/Scope/Detail/Lightbox/Mask 与 Markdown/KaTeX 样式改为按需加载。
- [x] 并发与生图性能专项已完成：Worker 支持环境变量并发覆盖，openai-compatible provider 有独立并发 cap；任务 attempts 可通过 REST/CLI/Web 查看；`vag benchmark image-generation` 可做 mock 和受控真实 provider 小样本压测。
- [x] P1 Provider Throughput & Reliability 已完成：真实 provider 默认 cap 收敛为 3，openai-compatible timeout profile 和 task attempt 阶段指标已接入；provider capability profile、同 prompt 多图拆分、diagnostic benchmark 和 batch progress 已可验证。
- [x] P1 Web Console Auth & Asset Visibility 已完成：Web 控制台支持轻量 Admin session，登录后可通过 Recent Assets 跨 scope 查看 MCP/CLI/REST/Web 生成的最近资产；project API key 仍用于外部 project 级调用；401、空列表和筛选无结果已在 Web 中区分展示。
- [x] P1 Project Production Context P1-PCTX-001 到 P1-PCTX-009 已完成：Project Visual Context 第一版复用 `project.metadata_json.visual_context`，支持角色卡、project reference binding、prompt recipe、CreateTask visual context 展开、REST/CLI/MCP 入口、任务/资产快照、自动化 examples、Web Project Context 面板、资产卡 Reference 动作、Web managed task context selector，并已通过 clean 萌宠故事 mock 回归验证 3 个 scene task / 6 张 assets 的 batch progress、asset list、Admin Recent Assets 和 metadata 追踪。
- [x] P1-BSE-003 Batch story summary API 已完成：REST `GET /api/projects/{project_id}/campaigns/{campaign_id}/batch-summary` 支持 `session_id/batch_id/story_id/source/status/include_setup/limit`，按 story/scene 聚合 scene production tasks、tasks 和 assets，返回 selected coverage、attempt/retry/error counts 和公开 delivery URLs，summary 不暴露 `local_path`。
- [x] P1-BSE-004 MCP list filters alignment 已完成：MCP `list_image_assets` 支持 `source/session_id/batch_id/status/keyword/limit`，透传到 `domain.AssetListQuery`，默认 project/campaign 行为保持不变，输出不暴露 `local_path`。
- [x] P1-BSE-005 Web production view read-only 已完成：Web 顶栏新增懒加载 Production View modal，复用 `batch-summary` API 展示 batch/story/scene counts、selected coverage、scene status、task/asset counts、thumbnail、delivery/metadata links、latest task id 和 error 摘要；401/403 显示 `unauthorized / login required`，刷新保留已有 summary。
- [x] P1-BSE-006 Scene asset actions 已完成：Production View scene asset cards 复用现有 select/reject asset review API；scene header 显示是否已有 selected asset；每个 asset 有 Select/Reject、per-asset pending/error 和 401/403 unauthorized 提示；成功后局部更新当前 summary，不关闭 modal、不整屏闪烁。
- [x] P1-BSE-007 Scene regenerate design 已完成：第一版采用 `create-new-task-as-regeneration`，复用 metadata-only lineage，不 retry 原 task、不覆盖旧 assets、不自动改变 selected/rejected；新 task 保持同一 `project_id/campaign_id/session_id/batch_id/story_id/scene_id`，并记录 `regenerated_from_task_id`、`regenerate_no`、reason、overrides 和 visual context snapshot 摘要。
- [x] P1-BSE-008 Scene regenerate implementation 已完成：REST `POST /api/projects/{project_id}/campaigns/{campaign_id}/scene-regenerations` 和 Web Production View Regenerate action 已接入，复用 `CreateTask` 生成新 task；旧 task/assets/selected/rejected 不覆盖，metadata 写入 `regenerated_from_task_id`、`regenerate_no`、reason/source/actor/overrides/source scene identity。
- [x] P1-BSE-009 Minimal export manifest 已完成：REST/CLI/Web 可导出 batch JSON manifest，支持 selected-only、all-assets 和 include-rejected 模式；manifest 复用 `batch-summary` grouping，包含 delivery/metadata URLs、target_path、task/scene/story/visual context 摘要，不返回 `local_path` 或 secret。
- [x] P1-BSE-010 Export pack ZIP boundary 已完成：P1 第一轮不实现服务端 ZIP，保留 JSON manifest + NAS/filesystem 访问为交付路径；未来 ZIP 必须另开切片并限制小批量 selected assets，加入数量、大小、路径和 secret 保护。
- [x] P1-BSE-011 NAS Docker access guide and regression docs 已完成：RUNBOOK 说明 Docker storage root / NAS bind mount / WebDAV / SMB / Finder 的只读访问边界；文件系统负责浏览、复制和备份，DB / metadata / manifest 负责状态、追踪、审核、visual context、manifest 和审计；不要手动移动、重命名或删除 selected / approved / published 资产；备份/恢复需要 Postgres dump 与 storage root 一致快照；应用内不实现 WebDAV/SMB server，也不扩成通用 DAM。
- [x] P1 Final Delivery / NAS Readable Export 第一轮 contract 已完成：`batch-manifest` 现支持 `view=engineering|final_delivery`，REST/CLI/Web 可导出带 `final_delivery.counts/stories/scenes/final_assets` 的 manifest；scene 最终交付图、caption 派生扁平字段、continuity/visual context 摘要与公开 delivery URLs 已可直接给人工复盘使用，且继续不暴露 `local_path`。
- [ ] P1 MCP Service Pack 当前为 partial：`docs/project/MCP_SERVICE_GUIDE.md`、`examples/mcp/agent-imageflow.local.json`、`examples/mcp/create-pet-scene.json`、`examples/mcp/smoke.md` 已新增；已完成 JSON parse 与静态检查，并补充 MCP 不承担 cleanup/reset 的删除边界；人工 MCP mock smoke evidence 尚未回填，所以暂不标 done。

## Remaining Product Gaps

- [x] MVP 产品闭环与生产 hardening 缺口已清零；当前仅剩本地开发环境清理类 follow-up，不属于产品缺口。
- [x] Post-MVP Project Visual Context P1 主线已完成：P1-PCTX-001 到 P1-PCTX-009 均已关闭；Batch Story Export Foundation 第一轮也已完成，后续 Usage Tracking、edit lineage、ZIP/多选下载、CLI/MCP regenerate command 或真实视觉质检需重新确认并独立拆分。
- [x] Web UX Smoothness 已完成 P1-UX-001 到 P1-UX-009：资产库稳定性、原子 scope 提交、Scope 统计后台加载、lazy modal fallback/preload、Task/Asset 卡片局部重绘和 production preview/browser 最终回归均已关闭。
- [x] Batch Story Export Foundation P1-BSE-001 到 P1-BSE-011 已完成：batch/story/scene 聚合查看、scene asset action、scene regenerate、JSON manifest、ZIP 后置边界和 NAS/Docker 文件访问说明均已收口，不扩成运营系统、通用 DAM 或内置 WebDAV/SMB server。
- [x] P2 Web Operator Review Console 已完成：Web 默认体验已从工程调试列表转成审图控制台，默认展示图片、prompt/剧情、story/scene/source/created/target 和状态；asset/task/scope/provider/hash/session/batch 等字段折叠到 `Technical details`；Settings 和文档明确 provider key/base URL 属于服务端平台能力，不变成每个登录用户自己的 provider 凭据。
- [x] 真实 provider 萌宠故事最小闭环已通过：在独立 project/campaign 下使用 `openai-compatible / gpt-image-2` 真实生成 2 个 scene、2 张 selected 资产，验证 Project Visual Context、prompt recipe、真实 provider、落盘、缩略图、metadata、Recent Assets 和 Production View 不断链。
- [x] P2 Web 审图追补与 MCP 真实 provider canary 已完成：`Project Context` 默认折叠为紧凑摘要，Recent Assets 标题更接近剧情/画面摘要，默认动作减少；MCP 工具入口已用 `openai-compatible / gpt-image-2` 完成 1 图低频 canary，不做 benchmark 或并发压测。
- [x] P1 Deployment Release Pipeline 已完成：GitHub Actions 可测试、构建并发布 GHCR 私有 API/Web 镜像；生产服务器通过 `docker-compose.prod.yml` 拉取镜像运行；Web 独立镜像、`.env.example.prod`、上线/更新/回滚/备份文档和部署静态检查已落地。
- [x] V1 baseline 已形成：当前 `main` 可作为第一版基线进入服务器/NAS 部署演练和真实试用观察；剩余任务和未来方向已收束到 `docs/project/V1_BASELINE_AND_ROADMAP.md`，后续不复开已完成的 P1/P2 CSV。
- [x] V1 本地项目文档入口已收敛：新增 `docs/project/README.md`、`issues/README.md` 和 `docs/project/stories/README.md`，新会话/新 agent 默认从这些索引进入；历史文档、stories 和 CSV 保留不移动、不删除。
- [x] 下一阶段产品发展计划已落地：新增真实萌宠业务试用、Story Continuity / Comic Workflow、Caption Edit Lineage 三个 CSV，以及 Story Continuity Agent guide 和 MCP 示例；明确连续叙事由额外 agent 负责，平台继续作为图片资产事实源。
- [x] Story Continuity 计划已按外部评审收敛为 MVC：新增 `issues/next-phase-p1-story-continuity-mvc.csv`，明确第一轮只做 3 格无字顺序生成、preflight、resolved reference assets、Production View 最小展示和 manifest 摘要；Story Review 新页面、Web 加字入口、批量 caption、ZIP 和自动视觉质检后置。
- [x] Story Continuity MVC 第一轮实现基线已完成：`CreateTask` 已支持 `story_context_v1`、panel 因果字段、reference_bindings / resolved_reference_assets 分离和 sequential preflight；Batch Summary / Manifest / Production View 已能展示最小连续性摘要。
- [x] V0.2 MCP Production Hardening 已完成本地 release gate：`V02-MCPH-011 Controlled pet business regression trial` 已在真实 provider 低并发下通过，证明 2 格情侣文案卡、3 格低背景连续故事、caption derivative final delivery、reference diagnostics、speaker/bubble anchor、panel state transition 与 final-delivery manifest/readability 已闭环；剩余的 HTTPS/Caddy、restore 和 `IMAGE_TAG` rollback 属于服务器部署演练门槛，不再阻塞 `v0.2.0` 代码发布。
- [ ] P1 Final Delivery / NAS Readable Export 仍有后续切片待做：第一轮 contract/manifest 已完成，但 `P1-DLV-004/005/006/007` 的 story/batch export pack、NAS readable mirror、project delivery defaults 和治理联动尚未实现，继续保持 canonical storage 不变与文件系统非事实源边界。
- [x] Caption Edit Lineage MCP-first 最小切片已完成：`metadata_json` 中的 `derived_from_asset_id`、`derivation_type`、`caption_text`、`caption_style`、`source_task_id` 和 `source_scene_id` 会形成 `caption_lineage` 摘要，进入 `structured_input_json`、provider parameters 和 batch manifest asset；manifest 继续不暴露 `local_path`。
- [x] Story Continuity MVC mock 数据链路已跑通：MCP/mock 3 panel 顺序任务完成，panel 2/3 正确记录上一格 selected asset，Batch Summary / selected Manifest 均返回 continuity 摘要；该证据只证明 metadata/status/select/manifest 数据链路，不证明视觉连续性。
- [ ] Story Continuity MVC 真实 provider canary 仍待业务确认：费用确认后按 cap=1、每格最多 2 候选、总图量最多 8 张执行，并由人工评分角色、场景、道具和因果连续性。
- [ ] P1 Server Deployment Rehearsal 已完成 Volcengine 旧服务更新 smoke：`main` 新镜像已 pull/up，临时 HTTP health/Web、MCP `tools/list` 和 mock benchmark smoke 通过，升级前 Postgres/storage/env/compose 备份已完成；HTTPS 同源入口、浏览器 Admin/delivery smoke、restore 和 `IMAGE_TAG` 回滚仍待执行。
- [x] P1 Web Console Auth Gate / Localization / Product Fit 已完成：Web 控制台从“进入后再局部登录资产库”改为“先 Admin 登录，再进入完整服务器托管控制台”；Help/About、Header、资产库、批次生产视图和项目视觉上下文主路径已中文化并收敛到 server-first 平台语义；Settings 信息架构后续独立设计。
- [ ] P1 Runtime/Auth/Accessibility/Lifecycle Closure 已完成本地上线前验收：`18079c7` 在本地 compose/browser 下已验证 Admin 登录门、runtime status、Current Scope 三个中文 `aria-label` 下拉、Recent Assets 缩略图、单 asset archive/restore、archived 默认保护和 MCP 无 destructive tools；mock 角色一致性 smoke 与 1 图真实 reference canary 也已跑通。剩余是部署环境 HTTPS/同源 replay、Basic Auth 开启场景复核和服务器 smoke 记录。
- [ ] P1 Character Reference Intake and Consistency 已完成本地验收：后端已支持 input-file 提升为正式 reference asset、角色主图/参考图绑定、CreateTask 自动带参考和 provider 参考图参与诊断；Web 角色卡已显示主图/参考图缩略图和缺图警告，`Project Context` 从资产卡打开时也已支持待绑定资产快捷区和角色一键绑定。本地 mock pet consistency smoke、Project Context browser smoke 和 1 图真实参考图 canary 已通过；剩余是部署环境复放和更完整的低并发真实工作流观察。
- [x] P1 Web Review Feedback and Stability 已完成 Web 前端与 tests 范围：`ServerAssetLibrary` 和 `ProductionViewModal` 已补 per-asset pending、optimistic status/style、失败回滚、局部 counts 更新、旧内容保留、请求去重/旧响应丢弃与 `429` 友好提示；未改 Go 后端，也未把角色图片绑定误标为 done。
- [ ] P1 Safe Delete and Trial Reset 已完成 CLI/REST foundation，并已补 Web cleanup entry：`vag storage cleanup-preview/execute` 和 Admin-only REST `storage-cleanup-preview/execute` 已支持按 asset/task/session/batch/story/campaign 定位未 protected 候选，继续使用 dry-run token/confirm、selected/published/approved 保护和 audit；Web `ScopeManagerModal` 已可对当前 campaign 执行 cleanup preview/execute，单 asset archive/restore 的本地 browser smoke 也已通过。剩余是 cleanup panel browser smoke、task/input-file 级 reset 和生产备份演练。
- [x] P1 Scope Management Usability Follow-up 已完成并补 browser smoke：非空 scope 删除已改为 Admin 受控级联删除，确认弹窗层级、Scope 管理滚动和输入框 `@` 误导文案已修复；MCP 仍不开放 destructive tools。

## Evidence Log

- 2026-06-27: 已完成 `V02-MCPH-011 Controlled pet business regression trial`，并据此关闭 `v0.2.0` 的本地功能 release gate。基于本地低并发真实 provider 环境，在 `project_id=prj_xiaobai_jimao_dog_dialogue_20260626130624`、`campaign_id=cmp_cp_dialogue_cards_20260626130624` 下执行两组受控业务回归：一组 2 格情侣文案卡 + caption derivative（dialogue session/batch/story 为 `v020_dialogue_session_20260627T124527 / v020_dialogue_batch_20260627T124527 / v020_dialogue_story_20260627T124527`），一组 3 格低背景连续故事（story session/batch/story 为 `v020_story_session_20260627T124527 / v020_story_batch_20260627T124527 / v020_story_20260627T124527`）。共 7 个 tasks 全部 completed 并 approved：`task_54c9e0a3341de92566bb -> asset_8f24aabd1f63c4b2cbb8`、`task_3883f47c90b34ca08332 -> asset_fad52365d2ebd8bd3dd5`、`task_6d6a843a77db9a6a9e68 -> asset_736d841a4ce928f73e63`、`task_e9720a7d3f269941eea4 -> asset_be095eabeaa7e0d94341`、`task_5bd73c179aa5b9bcc3fb -> asset_b260ecdb13a33f549201`、`task_fdcb199470082694a27b -> asset_61d3a44a1f083b4b7204`、`task_b372741024cacc443aa0 -> asset_e974a229eef0ca68aba7`。dialogue progress 为 `task_count=4 / succeeded_count=4 / partial_count=0 / failed_count=0 / asset_count=4`；story progress 为 `task_count=3 / succeeded_count=3 / partial_count=0 / failed_count=0 / asset_count=3`。selected `view=final_delivery` manifest 已能清楚区分最终交付图：2 格对话场景最终资产为 `asset_736d841a4ce928f73e63`、`asset_be095eabeaa7e0d94341`，3 格连续故事最终资产为 `asset_b260ecdb13a33f549201`、`asset_61d3a44a1f083b4b7204`、`asset_e974a229eef0ca68aba7`。其中 caption derivative task 已验证 `speaker_character_id`、`bubble_anchor`、`tail_direction`、`auto_select_derivative=true` 与 `delivery_role=final_delivery`；story panel 2/3 已验证 `previous_panel_asset_id`、`panel_index`、`emotion_after` 和 `provider_reference_participation=resolved_input_files`。5 个最终交付资产的 `download_url`、`thumbnail_url`、`metadata_url` 均返回 HTTP 200。本轮未读取或打印任何 provider key / API key / cookie / session / secret，也未触碰服务器升级、HTTPS/Caddy、restore 或 rollback。
- 2026-06-27: 已完成 P1 Final Delivery / NAS Readable Export 第一轮 `P1-DLV-001/002/003/008`：后端复用现有 `batch-manifest` 增加 `view=engineering|final_delivery`、`manifest_view` 和 `final_delivery` read model，按 `story/scene/final asset` 输出最终交付摘要、caption 派生扁平字段、continuity/visual context 和公开 delivery URLs；CLI `vag batch manifest --view final_delivery` 与 Web Production View `Final delivery manifest` 导出动作同步落地。验证：容器化 `go test ./internal/app ./internal/httpapi ./cmd/vag` 通过，`npm --prefix web test -- --run src/lib/agentImageflowApi.test.ts src/components/ProductionViewModal.test.ts` 通过；本轮未运行真实 provider，未读取或打印 key/secret/cookie/session。
- 2026-06-27: 已新增 `issues/next-phase-p1-final-delivery-nas-readable-export.csv`，专门解决“人工复盘和再次寻找困难”的交付层问题。范围明确为：保持 canonical storage `workspace/project/campaign/originals|thumbnails|metadata/asset_id` 不变，只补 final delivery contract、story/batch export 和 NAS readable mirror；不把文件系统重新提升为业务事实源，不扩成通用 DAM，不运行 provider，不读取或打印任何 key/secret/cookie/session。
- 2026-06-26: 已将 3 轮真实业务试跑暴露的 8 类问题收敛为 `v0.2 MCP Production Hardening` 计划：新增 `docs/project/V0_2_MCP_PRODUCTION_HARDENING.md`、`docs/project/V0_2_MCP_PRODUCTION_HARDENING_HANDOFF_PROMPT.md` 和 `issues/next-phase-v0-2-mcp-production-hardening.csv`，把下一版本默认入口明确为 agent-friendly context setup、reference diagnostics、caption speaker/bubble anchor、panel state transition、caption derivative delivery、provider partial success semantics、NAS storage/delivery governance、single asset summary 和 local Web review packaging。同步更新 `docs/project/README.md`、`issues/README.md`、`TASKS.md`、`PROJECT_STATUS_MAP.md`、`V1_BASELINE_AND_ROADMAP.md` 和 `DECISIONS.md`。本轮只做计划/CSV/文档落盘，不改 Go/TS 业务代码，不运行真实 provider，不连接服务器，不读取或打印 key/secret/cookie/session。
- 2026-06-26: 已完成 `V02-MCPH-002 Agent-friendly project/campaign/context setup API contract`：`internal/config` 新增 `AGENT_SETUP_TOKEN` 配置，`internal/httpapi` 新增 `X-Agent-Setup-Token` 授权逻辑和 bootstrap route allowlist，仅放开 `GET/POST /api/workspaces`、`GET/POST /api/workspaces/{workspace_id}/projects`、`GET/POST /api/workspaces/{workspace_id}/projects/{project_id}/campaigns`、`GET/POST /api/workspaces/{workspace_id}/projects/{project_id}/visual-context`；`cmd/vag` 支持 `AGENT_IMAGEFLOW_SETUP_TOKEN` 自动转发 header；`.env.example.prod`、`docs/project/MCP_SERVICE_GUIDE.md`、`examples/tasks/agent-setup-bootstrap-usage.md` 和 `docs/project/stories/slice-056-agent-friendly-setup-contract.md` 已同步。验证：容器化 `go test ./internal/config ./internal/httpapi ./cmd/api ./cmd/vag` 通过，CSV parse 与 `git diff --check` 通过；未运行真实 provider，未读取或打印 key/cookie/session/secret。
- 2026-06-26: 已完成 `V02-MCPH-003 Project Visual Context reference diagnostics`：`internal/app` 新增 project-level 和 snapshot-level diagnostics builder，输出 `image_backed`、`text_constrained`、`missing_environment_reference`、`weak_species_lock`、角色有图计数、缺图角色、species drift negative prompt 覆盖、identity signal 和 provider reference participation risk；GET project visual context 响应顶层返回 `reference_diagnostics`，task metadata / structured input 保留 `project_visual_context_diagnostics`，`internal/store` 会把它提取到 batch summary / manifest 的 `visual_context.reference_diagnostics`。Web `ProjectContextModal` 新增只读诊断卡和 helper tests。验证：容器化 `go test ./internal/app ./internal/store ./internal/httpapi ./internal/provider ./cmd/api ./cmd/vag` 通过；`npm --prefix web test -- src/lib/projectContextPanel.test.ts src/lib/agentImageflowApi.test.ts` 通过；`npm --prefix web run build` 通过；未运行真实 provider，未读取或打印 key/cookie/session/secret。
- 2026-06-26: 已完成 `V02-MCPH-004 Caption speaker / bubble anchor semantics`：`internal/domain` 扩展 `CaptionLineageSummary`，新增 `speaker_character_id`、`bubble_anchor`、`tail_direction`、`caption_intent` 和 `avoid_covering_subjects`；`internal/app` 会把旧根字段/新嵌套字段统一归一化到 `metadata_json.caption_lineage`，并把这些说话人/气泡语义追加成 provider 可见的 prompt 约束；`internal/mcp` 的 `create_image_task` schema 已显式描述 caption edit 嵌套 contract，`examples/mcp/create-caption-edit-task.json` 也改为保留原 `scene_id` 的 delivery variant 示例。验证：容器化 `go test ./internal/domain ./internal/app ./internal/provider ./internal/mcp` 通过；`npm --prefix web test -- src/lib/agentImageflowApi.test.ts` 通过；未运行真实 provider，未读取或打印 key/cookie/session/secret。
- 2026-06-26: 已完成 `V02-MCPH-005 Panel state transition and performance progression semantics`：`internal/domain` 扩展 `StoryPanelPlanEntry` 与 `BatchStoryContinuitySummary`，新增 `emotion_before`、`emotion_after`、`pose_change`、`relationship_shift`、`must_change`、`must_not_keep` 和 `state_transition_notes`；`internal/app` 会把这些字段回写到 task metadata，并追加成 provider 可见的 `State transition requirements` prompt 约束；`internal/store` 会把它们提取到 batch summary / manifest continuity，Web `ProductionViewModal` 也已补审图展示。`examples/mcp/create-story-context-v1.json` 已同步为新字段示例。验证：容器化 `go test ./internal/app ./internal/store ./internal/provider ./internal/mcp ./internal/httpapi ./cmd/api ./cmd/vag` 通过；`npm --prefix web test -- src/lib/agentImageflowApi.test.ts` 通过；`npm --prefix web run build` 通过；未运行真实 provider，未读取或打印 key/cookie/session/secret。
- 2026-06-26: 已完成真实萌宠账号业务推进的 MCP-first 工作流收敛：新增 `docs/project/PET_STORY_PRODUCTION_WORKFLOW.md`，明确外部 Story Continuity Agent 负责 Story Bible、Panel Plan、`story_context_v1`、reference choices 和 MCP 顺序编排；Agent ImageFlow 负责生成、追踪、Web 审图、select/reject、manifest 和 NAS/URL 交付，Web 不是主创作入口。新增 `examples/mcp/pet-story-production-plan.json` 作为 3-6 格故事计划示例；更新 `issues/next-phase-p1-pet-account-real-workflow-trial.csv`，把剩余 todo 从验收清单收敛为必须生产步骤和可选证据记录。同步更新 `TASKS`、`PROJECT_STATUS_MAP` 和 `V1_BASELINE_AND_ROADMAP`，说明下一步主线从 smoke 转为真实业务生产试用。本轮只改文档/示例/CSV，不改 Go/TS 业务代码，不运行 provider，不连接服务器，不读取或打印 key/secret/cookie/session。
- 2026-06-26: 已完成一次本地 5 格真实萌宠长链业务试跑并形成阶段门报告：在 `project_id=prj_xiaobai_jimao_ref_20260623094956` 下新建 `campaign_id=cmp_pet_story_longchain_trial_20260626104430`，由外部 Story Continuity Agent 产出 `story_context_v1` 与 5 格 panel plan，随后通过 MCP 顺序执行 `task_b540b5b4a62533d27f23`、`task_1c14b311378b553337a6`、`task_0d3d99f27afce3d9129d`、`task_ccd1c2c869df90af086b`、`task_8ff539f8fa8ab3e9246f`，selected assets 分别为 `asset_0336a2f32acc59559332`、`asset_b157000833e9979cee58`、`asset_bf127ae61fb2e7008483`、`asset_7f5c8b86e2de89d5f70c`、`asset_56c1ec7cc5fab2e3598c`。Batch summary / selected manifest 显示 5 scenes / 5 tasks / 10 assets / 5 selected，panel 2-5 均保留 `previous_panel_asset_id`，所有 selected assets 均含 `download_url/thumbnail_url/metadata_url`，`provider_reference_participation=resolved_input_files`，`caption_lineage` 缺失符合“未执行 caption edit”。本轮未做 regenerate，角色参考真实参与，但缺少独立环境 reference，因此只能标记为“角色 reference-assisted + 环境 text-constrained”。fresh Web preview 可打开登录页，但因未复用 Admin session，`/api/admin/me` 返回 `401`，未能在无凭据前提下完成 Production View replay；该问题已记录为审图入口复验摩擦。同步新增 `docs/project/pet-story-longchain-stage-gate-report-2026-06-26.md` 供 PM 重新评估下一阶段投入。
- 2026-06-26: 已完成一次本地 6 幅真实萌宠情侣向复杂场景试跑并形成阶段门报告：在 `project_id=prj_xiaobai_jimao_ref_20260623094956` 下新建 `campaign_id=cmp_pet_story_rainy_umbrella_cp_trial_20260626115435`，围绕“雨天共伞靠近”故事，通过 MCP 顺序执行 `task_8981b5543e36819aa37f`、`task_fbc2cb6cb03e9d0ef47d`、`task_0b84dfda8b6e003163e1`、`task_87719cb68a84fbd1036c`、`task_7fa7fe33bb5a046c5a28`、`task_a0eb92768fd0f4674fd3`、`task_d008362900cba74d23dd`，最终 selected assets 为 `asset_b51c304625ba1f1bae6d`、`asset_7e99bd54b26aa250398d`、`asset_cebeb538195aa88da860`、`asset_859797f0d544c2dcc66c`、`asset_0602f95a1da2d12c3436`、`asset_99e1ea0ddcf8e9b01072`。本轮共 7 tasks / 12 张实际输出 / 6 张 selected / 1 次单格 regenerate；panel 2-6 均保留 `previous_panel_asset_id`，角色 reference 与 previous-panel reference 均真实参与，环境仍缺少独立 reference，因此继续标记为“角色 reference-assisted + 环境 text-constrained”。selected manifest 的 asset continuity 已包含 `panel_index`、`previous_panel_asset_id`、`provider_reference_participation`，但 `delivery` 字段为空，本轮额外用 `get_asset_delivery_info` / asset endpoints 确认 6 个 selected assets 的 original/thumbnail/metadata URL 全部返回 200。fresh Web preview 仍停在 Admin 登录页，未在无凭据前提下完成 Production View replay；同时已验证 6 张单格 selected assets 可后处理组合成 3 张上下两格发布版，但这只证明交付组织可行，不代表平台原生支持双分镜创作。同步新增 `docs/project/pet-story-rainy-umbrella-cp-stage-gate-report-2026-06-26.md` 供 PM 重新评估下一阶段投入。
- 2026-06-26: 已完成 P1 Caption Edit Lineage MCP-first 最小切片：新增 `CaptionLineageSummary`，从 task `metadata_json` 提取并写入 `structured_input_json.caption_lineage`，provider parameters 输出 `caption_lineage`，batch summary / manifest asset 透传 `caption_lineage`；新增 `examples/mcp/create-caption-edit-task.json`，使用原 asset_id + `role=edit_target` + mock provider，不包含真实 key。验证：容器化 `go test ./internal/provider ./internal/app` 通过；本轮未运行真实 provider，未读取或打印 key/secret/cookie/session。
- 2026-06-25: 已完成 Story Continuity MVC 第一轮实现基线：新增 `internal/app/story_continuity.go` 及配套 domain/store/provider/web 改动，`CreateTask` 现可解析 `metadata_json.story_context_v1`，展开 reference bindings，写入 panel causality metadata、resolved reference assets、continuity warnings，并对 sequential previous-panel flow 执行 preflight；Batch Summary / Manifest / Production View 已展示 continuity 摘要；`examples/mcp/run-3-panel-story-smoke.md` 已改为 `selection_mode=manual_optional` 的顺序 smoke。验证：容器化 `go test ./internal/app ./internal/store ./internal/provider` 通过；`npm --prefix web test -- --run src/lib/agentImageflowApi.test.ts src/lib/reviewFeedback.test.ts` 通过。本轮未运行真实 provider，未读取或打印 key/secret/cookie/session。
- 2026-06-26: 已用当前源码重建本地 API/worker，并完成 Story Continuity MVC 的 MCP/mock 3 panel 数据链路模拟：`story_mcp_mock_session_1782439181` / `story_mcp_mock_batch_1782439181` 下，panel 1/2/3 tasks `task_b6366865dae01c7d1fb2`、`task_609b13721fc8e022a3de`、`task_a286252146e5508a7681` 均 completed；selected assets `asset_57f2ad1aa891bb32a9c9`、`asset_2157fa72781d0f35f432`、`asset_9a0d44047b88f800b322`。Batch Summary 返回 3 scenes / 3 tasks / 6 assets / 3 selected，并在 scene continuity 中包含 `panel_index`、`previous_panel_asset_id` 和 `provider_reference_participation=not_requested`；selected Manifest 同样输出 asset continuity。该 mock 只证明数据链路，不证明视觉连续性。本轮未运行真实 provider，未读取或打印 key/secret/cookie/session。
- 2026-06-25: 已完成下一阶段产品发展计划落地：新增 `issues/next-phase-p1-pet-account-real-workflow-trial.csv`、`issues/next-phase-p1-story-continuity-comic-workflow.csv`、`issues/next-phase-p1-caption-edit-lineage.csv`，新增 `docs/project/STORY_CONTINUITY_AGENT_GUIDE.md`、`examples/mcp/story-continuity-agent.local.json`、`examples/mcp/create-story-bible.json`、`examples/mcp/create-panel-plan.json` 和 `examples/mcp/run-3-panel-story-smoke.md`；同步 `issues/README.md`、`docs/project/README.md`、`V1_BASELINE_AND_ROADMAP.md`、`PROJECT_STATUS_MAP.md`、`TASKS.md`、`DECISIONS.md` 和 `CHECKPOINTS.md`。本轮只做计划与接入材料，不改业务代码，不运行真实 provider，不连接服务器，不读取或打印 key/secret/cookie/session。
- 2026-06-25: 已根据外部评审和 5 并发真实试用结果完成 Story Continuity 修订计划落盘：新增 `issues/next-phase-p1-story-continuity-mvc.csv`、`examples/mcp/create-story-context-v1.json` 和 `docs/project/STORY_CONTINUITY_MVC_HANDOFF_PROMPT.md`；更新 `issues/README.md`、`STORY_CONTINUITY_AGENT_GUIDE.md`、`V1_BASELINE_AND_ROADMAP.md`、`TASKS.md`、`PROJECT_STATUS_MAP.md`、`DECISIONS.md` 和 `CHECKPOINTS.md`。修订明确：Mock 只验证数据链路，视觉连续性需 cap=1、最多 8 张真实 provider canary 和人工评分；`reference_bindings` 与 `resolved_reference_assets` 必须分离；Web Story Review 新页面、加字 UI、批量 caption、ZIP 和视觉质检后置。本轮不改业务代码，不运行真实 provider，不连接服务器，不读取或打印 key/secret/cookie/session。
- 2026-06-25: 已完成 V1 本地项目收敛：新增 `docs/project/README.md` 作为项目文档入口、`issues/README.md` 作为 CSV 索引、`docs/project/stories/README.md` 作为历史 slice 索引；`TASKS.md`、`PROJECT_STATUS_MAP.md`、`V1_BASELINE_AND_ROADMAP.md` 和 `CHECKPOINTS.md` 已同步 `v0.1.0` baseline 与后续维护主线。该轮不移动、不删除历史文档、stories 或 CSV，不清理本地缓存，不改业务代码，不运行真实 provider，不读取或打印任何 key/secret/cookie/session。
- 2026-06-25: 已完成 `18079c7 Close runtime diagnostics and asset lifecycle gaps` 本地上线前验收。基线验证全部通过：容器化 `go test ./...`、`npm --prefix web test -- --run` 25 files / 264 tests、`npm --prefix web run build`、开发/prod compose config 和 `python3 scripts/check_deployment_release.py` 均通过；本地 `healthz` 返回 ok，production preview 可访问。当前本地 `.env` 未配置可用的 Admin 登录项，因此为浏览器验收临时使用一次性本地 Admin 配置重启 compose，不输出任何 secret。Web smoke 已验证未登录只见登录页、登录后可见 runtime status、Current Scope 下拉中文 `aria-label`、Recent Assets 缩略图，以及 mock 资产 `asset_e7e35e5f33d8fd67896c` 的 Select -> Reject -> Archive -> Restore 生命周期；archived 状态下 `Select/Reject` 按钮禁用，需先 `Restore`。MCP `tools/list` 仍只有 `create_image_task/get_image_task/list_image_assets/select_image_asset/reject_image_asset/get_asset_delivery_info` 六个安全工具。角色一致性链路本地已完成 mock smoke：`task_bd13ef56708ef0aeb5d8 -> asset_38c9ef3c6e3cc5e1219e` 保留 `visual_context_snapshot`、`character_ids`、`reference_asset_count=3`、`provider_reference_participation=resolved_input_files`，Web `Project Context` 可见两只萌宠角色主图/参考图。真实 provider 仅跑 1 张 reference canary：`openai-compatible / gpt-image-2` 的 `task_37d88b6014d863eb202a -> asset_5f5e2cff874f074e344e` completed，original/thumbnail/metadata URL 均返回 200，batch summary/manifest 与 Web Production View 均可用。本轮真实 provider 总量 1 张，未输出任何 key/secret/cookie/session；非阻塞观察是本地 compose 未注入 API build metadata 时 runtime status 显示 `API unknown`，建议在线上 GHCR 镜像环境再复核一次 build 摘要。
- 2026-06-26: 已完成 `V02-MCPH-006 Caption derivative delivery semantics`。`caption_lineage` 现已支持 `auto_select_derivative`，服务端会在 `manual_optional` caption derivative task 中按该标志自动选中第一张派生图；`batch summary` / `batch manifest` 新增只读 `delivery_role`，并在 `selected_only` manifest 中把 `base_original`、`caption_derivative` 与 `final_delivery` 折叠清楚，不再要求运营手动二次 select 才能切到带字交付图。验证：容器化 `go test ./internal/domain ./internal/app ./internal/provider ./internal/mcp` 通过，`npm --prefix web test -- src/lib/agentImageflowApi.test.ts` 通过，`python3 -m json.tool examples/mcp/create-caption-edit-task.json` 通过；未运行真实 provider，未读取或打印任何 key/cookie/session/secret。
- 2026-06-26: 已完成 `V02-MCPH-007 Provider partial success product semantics` 与 `V02-MCPH-009 Single asset readable production summary`。后端 task/runtime semantics 现在会统一输出 `requested_count`、`delivered_count`、`partial_success_reason`、`provider_error_summary`；单资产 `asset` / `metadata` 响应新增 `asset_summary`，收敛 story/scene/panel、dialogue/caption、derived_from、previous_panel、reference participation、provider/model、asset status 和 delivery role。Web 审图标题、概览和技术详情已直接消费 `asset_summary` 与 `caption_lineage`，并继续屏蔽 `local_path`、cookie、token、provider key、project key 等敏感信息。验证：容器化 `go test ./internal/domain ./internal/app ./internal/provider ./internal/mcp` 通过，`npm --prefix web test -- src/lib/agentImageflowApi.test.ts src/lib/operatorReview.test.ts` 通过，`npm --prefix web run build` 通过；未运行真实 provider，未读取或打印任何 key/cookie/session/secret。
- 2026-06-26: 已完成 `V02-MCPH-008 NAS storage adaptation and delivery governance` 与 `V02-MCPH-010 Local Web review packaged environment` 第一版文档/运维收口。`RUNBOOK.md` 与 `SERVER_DEPLOYMENT_GUIDE.md` 已明确：DB/metadata/manifest 是事实源，storage root 通过 bind mount 挂到持久目录或 NAS，本地/NAS 浏览建议只读，禁止手动移动/重命名平台管理目录；备份必须同时包含 Postgres dump 与 storage root 一致快照。本地 packaged review 固定走 `docker compose up -d postgres redis api worker` + `npm --prefix web run build` + `npm --prefix web run preview -- --host 127.0.0.1 --port 4173`，并附带按 `session_id` / `batch_id` / `story_id` replay Recent Assets、Production View、batch summary 与 batch manifest 的标准步骤。验证：`docker compose -f docker-compose.prod.yml config --quiet` 通过，`npm --prefix web run build` 通过，docs review 完成；未运行真实 provider，未读取或打印任何 key/cookie/session/secret。
- 2026-06-25: 已完成 P1 Runtime/Auth/Accessibility/Lifecycle Closure 的主要代码项并新增 `issues/next-phase-p1-runtime-auth-accessibility-lifecycle-closure.csv`：Admin runtime status 增加安全 `build` 摘要，Dockerfile/Dockerfile.web/GitHub Actions 注入 version/commit/build time/image tag，Web 显示 API/Web build 状态并提示 commit mismatch；登录失败文案区分 Admin 账号错误、接口不匹配和网络错误；Current Scope workspace/project/campaign 下拉补中文 `aria-label`；单 asset 新增 Admin-only `archive/restore` REST、Web 卡片动作和 `vag asset archive/restore`，归档对外显示 `archived`，恢复回 `generated`；默认 storage cleanup 不再包含 archived/deprecated，Web cleanup 面板也默认保护 archived。验证：容器化 `go test ./...` 通过；`npm --prefix web test -- --run` 25 files / 264 tests 通过；`npm --prefix web run build` 通过且仅有既有 chunk warning；开发/prod compose config 通过；`python3 scripts/check_deployment_release.py` 与 `git diff --check` 通过。本地 browser smoke、mock 角色一致性 smoke 和 1 图真实 reference canary 已在同日上线前验收中补齐；剩余是部署环境 replay 和服务器证据回填。
- 2026-06-25: 已完成 P1 Scope Management Usability Follow-up 与 MCP Service Pack 收口：Postgres scope 删除改为显式级联删除 `delivery_event/review_event/asset_version/asset/task_attempt/generation_task/campaign/project/workspace`，Service 层继续在 DB 删除后清理 storage scope 目录；ScopeManager 删除确认文案改为级联删除并展示 project/campaign/task/asset/selected/published 影响范围，ConfirmDialog 提升到 `z-[160]`，ScopeManagerModal 改为视口内可滚动布局，InputBar placeholder 移除未实现的 Project Reference `@` 承诺。MCP Service Pack 示例改为单行 JSON-RPC、默认 seed scope，mock smoke 完成 `tools/list -> create_image_task -> get_image_task -> list_image_assets -> get_asset_delivery_info`：`task_2c3706988499001b9715` completed，产出 `asset_6ba86dbe71f8c5ac4a57` 和 selected `asset_bf3a0db6a83f7542c1b7`，delivery 返回 `download_url/thumbnail_url/metadata_url`；`tools/list` 仍只有 6 个安全工具，无删除类工具。验证：`npm --prefix web test -- --run` 24 files / 262 tests 通过，`npm --prefix web run build` 通过且仅有既有 chunk warning，容器化 `go test ./...` 通过，开发/prod compose config 通过，`git diff --check` 通过。Browser smoke：创建非空 campaign `cmp_smoke_delete_1782353313` 与 selected asset `asset_c214e3745f97c30afd6b`，Web ScopeManager 确认框显示 `1 任务、1 资产、1 已选` 且“级联删除”按钮可点击；重建当前 API/worker 后经 Web 删除成功，campaign 从下拉消失并回退默认 campaign，asset metadata/original 返回 404，MCP list deleted campaign 返回 `[]`；761x937 viewport 下 ScopeManager 主体 scroller `clientHeight=520`、`scrollHeight=1822`，滚动到底可到达数据清理区域。本轮未运行真实 provider，未读取或打印 API key/provider key/secret/cookie/session。
- 2026-06-23: 已记录 Scope 管理真实试用 follow-up：新增 `issues/next-phase-p1-scope-management-usability-followup.csv`，覆盖 non-empty workspace/project/campaign 级联删除、ConfirmDialog z-index、ScopeManagerModal 滚动和 InputBar `@` placeholder 文案收敛。用户已确认删除整个 scope 时 selected/approved/published 资产也应随 scope 删除；该能力后续仍只走 Admin Web/REST/CLI，不进入 MCP destructive tools。本轮只更新项目管理文档，不实现业务逻辑，不执行删除，不运行 provider，不读取或打印任何 key/secret/cookie/session。
- 2026-06-23: 已在 Volcengine VPS 更新旧版 Agent ImageFlow 服务：升级前完成 Postgres dump `agent_imageflow_20260623T062145Z.sql`、storage 快照 `agent_imageflow_storage_20260623T062145Z.tar.gz`、compose/env 备份；`docker compose --env-file .env.prod pull api worker web && up -d` 后 API 镜像为 `9637de835ac6`，Web 镜像为 `5e237d3f11f1`，API/Postgres/Redis healthy，Worker/Web running；公网 `http://163.7.5.68:18080/healthz` 与 `:18081/healthz` 返回 `200 {"status":"ok"}`，Web 首页返回 `200 text/html`；MCP `tools/list` 返回 6 个工具；mock benchmark `vps_update_smoke_20260623T062530Z` 完成 1 task / 1 asset / 0 failed，`task_c395196e3db93a6788ea -> asset_0a0efbb532fa943e0f92`，asset version ready 且 thumbnail/metadata 已登记；磁盘 `/dev/vda2` 使用率 `27%`。本轮未配置 HTTPS/Caddy 正式入口，未做浏览器 Admin delivery smoke、restore 或 `IMAGE_TAG` 回滚，未运行真实 provider，未读取或打印 `.env.prod`、GHCR token、provider key、project key、Basic/Auth/Admin cookie 或 session。
- 2026-06-23: 已完成 P1 Server Deployment Rehearsal 准备包：新增 `issues/next-phase-p1-server-deployment-rehearsal.csv` 和 `docs/project/stories/slice-053-server-deployment-rehearsal.md`，把 GHCR pull、prod compose first boot、HTTPS 同源入口、Admin Web mock task、MCP stdio smoke、Postgres + storage restore、`IMAGE_TAG` 回滚和可选 1 图真实 provider canary 拆成独立验收项；`docs/project/SERVER_DEPLOYMENT_GUIDE.md` 补部署演练证据模板，`RUNBOOK`、`TASKS`、`PROJECT_PLAN`、`V1_BASELINE_AND_ROADMAP`、`PROJECT_STATUS_MAP` 和 `DECISIONS` 已同步。验证：`python3 scripts/check_deployment_release.py` 通过，`docker compose -f docker-compose.prod.yml --env-file .env.example.prod config` 通过，CSV parse 确认 8 行与必需列，`git diff --check` 通过。本轮未连接真实服务器，未读取或打印 `.env.prod`、GHCR token、provider key、project key、Basic/Auth/Admin cookie 或 session，也未运行真实 provider。
- 2026-06-23: 已完成 OpenAI-compatible 参考图 MIME 修复验收并提交：`/images/edits` multipart 的 `image[]` 与 `mask` part 现在显式写入 `Content-Type`，避免兼容 provider 将参考图识别为 `application/octet-stream`；新增 provider 回归测试确认 reference image 和 mask part 都为 `image/png`。验证：`go test ./internal/provider -count=1` 通过，容器化 `go test ./...` 通过，`git diff --check` 通过。真实 provider canary 未在本轮执行；后续必须先部署包含该修复的镜像/commit，并由用户明确确认费用后，再按 RUNBOOK 的 Reference image 1 图 canary checklist 执行。本轮未读取或打印任何 API key / provider key / secret / cookie / session。
- 2026-06-23: 已完成 P1 Character Reference Intake Web 快捷绑定（Task 3）：新增 `web/src/lib/projectContextQuickBinding.ts` 与 `projectContextQuickBinding.test.ts`，把“设为角色主图 / 加入角色参考图 / 保存为项目参考图”收敛为可测 helper；`ProjectContextModal` 在由资产卡带 `asset_id` 打开时会显示“待绑定资产”紧凑区，展示缩略图、asset_id、角色选择和三种快捷动作；没有角色卡时会明确提示先创建角色并可直接切到角色页；原有高级 reference 表单继续保留。验证：`npm --prefix web test -- --run src/lib/projectContextQuickBinding.test.ts` 通过。真实 provider canary、完整 mock pet consistency smoke 和 browser smoke 本轮未执行，也未被标记完成；本轮未运行真实 provider，未读取或打印任何 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1 Web Console Auth Gate / Localization / Product Fit：新增并关闭 `issues/next-phase-p1-web-console-auth-localization-product-fit.csv`；新增 `ConsoleLoginScreen` 和 `consoleAuth` helper/test，App-level gate 在未登录时只渲染登录页，登录后进入完整控制台，退出回到登录页；资产库移除局部登录表单；HelpModal 改为 Agent ImageFlow 控制台指南；Settings About 改为当前项目与上游致谢分离；主路径 UI 文案中文化；本地 Vite preview/dev origin `4173/5173` 会按当前 host 回退到 `127.0.0.1:8081` 或 `localhost:8081`，避免 Admin cookie host 不一致，生产 Web 镜像仍同源访问 `/api`。验证：`npm --prefix web test -- --run` 20 files / 243 tests 通过，`npm --prefix web run build` 通过且仅有既有 chunk warning，容器化 `go test ./...` 通过，`docker compose config --quiet` 与 prod compose config 通过，`git diff --check` 通过。本轮未运行真实 provider，未读取或打印 provider key / API key / secret / cookie / session。
- 2026-06-22: 已追加 P1 Character Reference Intake and Consistency 计划：新增 `issues/next-phase-p1-character-reference-intake-consistency.csv`，记录真实试用中“角色卡无图、input-files 未沉淀为 asset、纯文生图成功被误判为参考图成功、provider reference 诊断不足”的缺口；后续验收需区分平台链路成功、参考图参与成功和角色一致性人工判断通过。本轮只更新计划，不运行真实 provider，不读取或打印 provider key / API key / secret / cookie / session。
- 2026-06-22: 已完成 P1 Character Reference Intake 后端子任务（子代理 B）：新增 `POST /api/workspaces/{workspace_id}/projects/{project_id}/campaigns/{campaign_id}/input-files/{input_file_id}/promote-asset`，把当前 scope 的 input-file 登记为正式 asset/version/thumbnail/metadata；扩展 Character Profile 的 `reference_policy` 和 `appearance_lock_notes`，继续校验角色主图/参考图 asset 属于同 workspace/project；CreateTask 展开角色 visual context 时自动将 primary/reference assets 放入 `reference_images` 并解析为 provider 可用输入；`structured_input_json` 和 asset `parameters_json` 记录 `reference_asset_count`、`reference_input_file_count`、`provider_reference_participation`、source 和 MIME 诊断；provider 参考图读取失败错误包含“参考图未参与生成”语义。本轮只跑 Go tests/mock/fake，不运行真实 provider，不读取或打印 provider key / API key / secret / cookie / session；Web 可视化确认仍未完成。
- 2026-06-22: 已追加 P1 Web Review Feedback and Stability 计划：新增 `issues/next-phase-p1-web-review-feedback-stability.csv`，记录真实试用中 Select/Reject 状态反馈不明显、Production View 局部更新不清晰和下拉切换整页闪烁的问题；这是 P1 Web UX Smoothness 后的 follow-up，不复开旧 CSV。本轮只更新计划，不运行服务端或 provider。
- 2026-06-22: 已完成 P1 Web Review Feedback and Stability 的 Web 前端与 tests 范围：新增 `web/src/lib/reviewFeedback.ts` 与 `web/src/lib/reviewFeedback.test.ts`，把 optimistic review、失败回滚、scene/story/top counts 局部更新和 `429` 友好提示收敛为可测 helper；`ServerAssetLibrary` 现已在 Recent Assets / 当前空间中保留旧内容、显示 per-asset pending 与错误、过滤后仍能正确反映 optimistic status；`ProductionViewModal` 的 scene asset Select/Reject 会局部更新 asset card、scene header 和 selected coverage，不再依赖整屏重拉。验证：`npm --prefix web test -- --run`、`npm --prefix web run build`、`git diff --check` 通过。本轮未运行真实 provider，未读取或打印任何 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1 Character Reference + Safe Delete foundation 收口：Web Project Context 角色卡新增主图/参考图缩略图、缺图警告、`reference_policy` 和 `appearance_lock_notes` 编辑显示，便于确认角色是否真的有图；`vag storage cleanup-preview/execute` 增加 `--asset-id/--task-id/--session-id/--batch-id/--story-id/--deprecated`，Admin-only REST 新增 `storage-cleanup-preview/execute`，继续复用 dry-run token、selected/published/approved 保护和 audit；MCP Service Guide 明确 MCP 不提供删除/清理工具，删除和试用重置走 Admin Web/REST/CLI。验证：容器化 `go test ./...` 通过；`npm --prefix web test -- --run` 21 files / 249 tests 通过；`npm --prefix web run build` 通过且仅有既有 chunk warning；`docker compose config --quiet` 与 prod compose config 通过。本轮未运行真实 provider，未读取或打印任何 API key / provider key / secret / cookie / session。
- 2026-06-22: 已追加 P1 Safe Delete and Trial Reset 计划：新增 `issues/next-phase-p1-safe-delete-and-trial-reset.csv`，并同步 TASKS、PROJECT_PLAN、PROJECT_STATUS_MAP、V1_BASELINE_AND_ROADMAP、NEXT_PHASE_REQUIREMENTS、FUTURE_REQUIREMENTS_AND_SCENARIOS 和 DECISIONS。该计划只记录后续切片，不实现删除逻辑，不执行清库或 provider 调用；第一轮建议 Admin Web/REST/CLI 受控删除和 batch/campaign 重置，MCP 不开放 workspace/project/campaign 硬删除。
- 2026-06-23: 已完成 P1 Safe Delete and Trial Reset 的 Web 数据清理入口（Task 2）：`ScopeManagerModal` 新增当前 campaign 的“数据清理”面板，复用现有 Admin-only REST `storage-cleanup-preview/execute`，展示 `candidate_count/file_count/bytes/by_reason`、`protected selected/published` 和截断 dry-run token；execute 需要输入“清理当前空间”，未 preview 不可执行，并对 `401/403/429` 给出可理解提示；不显示本地路径或完整 token，也未新增 MCP 删除工具、未改后端删除语义。验证：`npm --prefix web test -- --run` 23 files / 258 tests 通过，`npm --prefix web run build` 通过且仅有既有 chunk warning。本轮未运行真实 provider，未读取或打印任何 key/secret/cookie/session；后续本地上线前验收已补单 asset archive/restore browser smoke，cleanup panel 本身的浏览器回放仍待后续补齐。
- 2026-06-22: 已形成 V1 baseline and roadmap：新增 `docs/project/V1_BASELINE_AND_ROADMAP.md`，并同步 README、PROJECT_PLAN、PROJECT_STATUS_MAP、TASKS、FUTURE_REQUIREMENTS_AND_SCENARIOS、NEXT_PHASE_REQUIREMENTS 和 CHECKPOINTS。当前后续主线为 server deployment rehearsal、pet account real workflow trial、usage/edit lineage、export pack ZIP 和 deployment secret hardening；账号运营、小红书发布、通用 DAM、多人协作、SaaS 注册计费、每用户 provider key、视觉质检 AI 和无目标真实 provider benchmark 继续后置。本轮只更新文档，不读取或打印 provider key / API key / secret / cookie / session，不运行真实 provider。
- 2026-06-22: 已启动 P2 Web Operator Review Console，新增 `issues/next-phase-p2-web-operator-review-console.csv`、`docs/project/stories/slice-051-web-operator-review-console.md` 和 `docs/superpowers/plans/2026-06-22-web-operator-review-console.md`；DECISIONS 确认 server-first provider/auth 语义，PROJECT_PLAN/PROJECT_STATUS_MAP/TASKS 已切到 P2 主入口。本阶段不运行真实 provider，不读取或打印任何 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P2 Web Operator Review Console：`issues/next-phase-p2-web-operator-review-console.csv` 中 P2-ORC-001 到 P2-ORC-008 均为 done；新增 `web/src/lib/operatorReview.ts` 和回归测试，`ServerAssetLibrary` 默认审图卡片隐藏长 ID，`SettingsModal` 收束服务端平台连接语义，`ProductionViewModal` 支持资产卡 `Batch` seed 入口；浏览器 smoke 确认 Recent Assets 默认文本不含 `Asset ID` / `Task ID` / `Hash` / `Provider` / `Model`，`Technical details` 默认折叠，Production View 可带入 batch/session filters，页面未显示 secret-like labels。验证：`npm --prefix web test -- --run src/lib/operatorReview.test.ts` 4 tests 通过，`npm --prefix web test -- --run` 18 files / 232 tests 通过，`npm --prefix web run build` 通过且仅有既有 chunk warning，`git diff --check` 通过。本轮未运行真实 provider，未读取或打印任何 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成真实 provider 萌宠故事 smoke：clean batch `prj_real_pet_story_1782108549 / cmp_real_pet_story_1782108549 / real_pet_story_session_1782108549 / real_pet_story_batch_1782108549` 使用 `openai-compatible`，model `gpt-image-2`，创建 `task_d306b60a9a4342a9d700` 和 `task_252f3bad2d988c5287a4`，均 completed，各生成 1 张 selected asset：`asset_adeb35a6643c8f263848`、`asset_e3eb7dec1fad26e09d1e`。Batch summary 返回 `scene_count=2`、`scene_with_selected_count=2`、`failed_count=0`、`asset_count=2`；thumbnail URLs 返回 `image/webp`，original URLs 返回 `image/png`；metadata URL 不暴露本地路径且保留 `visual_context_snapshot`；Web Recent Assets 可见真实资产，资产卡 `Batch` 可打开 Production View 并带入 session/batch/story filters，Query 后显示 `selected coverage=2/2`。一次较早的 zsh 脚本错误在失败前已发出 1 个额外真实 scene_001 请求并产生同类资产，该资产不计入 clean batch 结论。本轮未读取或打印任何 provider key / API key / secret / cookie / session。
- 2026-06-22: 已完成 P2 Web 审图追补与 MCP 真实 provider canary：`issues/next-phase-p2-web-operator-review-console.csv` 新增并关闭 P2-ORC-009 到 P2-ORC-011；`Project Context` 默认折叠为一行摘要，`Expand` 后才显示 recipe/characters/references 细节；Recent Assets 标题使用 `getAssetReviewTitle`，优先剧情/画面摘要并收敛 recipe-expanded prompt；默认卡片动作减少，低频 copy/reference/scope/metadata 放入 `Technical details`。MCP canary 使用独立 scope `prj_mcp_real_pet_canary_1782109391 / cmp_mcp_real_pet_canary_1782109391`、session/batch `mcp_real_pet_canary_session_1782109391 / mcp_real_pet_canary_batch_1782109391`，provider `openai-compatible`、model `gpt-image-2`，生成 `task_b5256922a91e424850d3 -> asset_7c706c1a1cea00490a40`；batch progress 返回 `task_count=1`、`succeeded_count=1`、`failed_count=0`、`asset_count=1`、`selected_asset_count=1`，metadata 保留 `visual_context_snapshot`，thumbnail/original URL 可访问。验证：focused tests 2 files / 7 tests 通过，Web full tests 19 files / 235 tests 通过，Web build 通过且仅有既有 chunk warning，browser smoke 确认 Project Context 默认折叠和卡片主视图收敛。本轮未读取或打印任何 provider key / API key / secret / cookie / session。
- 2026-06-22: 已完成 P1 Deployment Release Pipeline：新增 `issues/next-phase-p1-deployment-release-pipeline.csv`、`docs/project/stories/slice-052-deployment-release-pipeline.md`、`docs/project/SERVER_DEPLOYMENT_GUIDE.md`、`docker-compose.prod.yml`、`Dockerfile.web`、`docker/nginx-web.conf`、`.github/workflows/docker-publish.yml`、`.env.example.prod` 和 `scripts/check_deployment_release.py`。发布流采用 GHCR 私有镜像：`ghcr.io/billionsheep/agent-imageflow-api` 与 `ghcr.io/billionsheep/agent-imageflow-web`，版本 tag 支持 `main`、`vX.Y.Z` 和 `sha-<short_sha>`；prod compose 不含 `build:`，Postgres/Redis 不暴露 ports，API/Web 默认只绑定 `127.0.0.1` 给反向代理；`.env.prod` 只在服务器保存。验证：deployment static check 通过，开发/prod compose config 通过，Web tests/build 通过，容器化 Go tests 通过，API/Web Docker image build 通过，Web image HTTP smoke 返回 200，API health 返回 ok，MCP stdio `initialize -> tools/list` 返回工具列表。本轮未读取或打印 provider key / API key / secret / cookie / session，未运行真实 provider。
- 2026-06-18: 根据用户讨论，项目从知识库图示生产系统收敛/转向更通用的生图自动化资产平台。
- 2026-06-18: 输入/输出和业务隔离 v0.1 已冻结，保留未来业务扩展点但不扩大 MVP。
- 2026-06-18: 已将架构评审合并进 `ARCHITECTURE.md`，补齐状态模型、幂等重试、一致性边界、文件访问隔离、provider 失败模型和演进触发条件。
- 2026-06-18: 已初始化 Git 仓库，绑定并推送到 `git@github.com:billionsheep/agent-imageflow.git`。
- 2026-06-18: 已新增 `IMPLEMENTATION_REVIEW_AND_FLOW_SIMULATION.md`，模拟内容账号 campaign 封面图生成、候选选择、交付和失败路径。
- 2026-06-18: 已确认进入实施准备阶段；第一阶段使用 Go、PostgreSQL、Redis、本地文件系统、Docker Compose 和 mock provider，不考虑本地 GPU。
- 2026-06-18: 已回退低保真自写实现，改为基于 `GPT Image Playground` 导入 `web/` 并二开；Web 测试和构建通过。
- 2026-06-18: 已完成服务端 mock 资产闭环：`docker compose up` 启动 API/Worker/PostgreSQL/Redis；CLI 创建任务后 Worker 生成 3 个 ready asset_version；approve 兼容命令可标记推荐资产并返回 original/thumbnail/metadata URL。
- 2026-06-18: 已明确 Web 与服务端不是长期并行的两套正式系统；最终 Web/MCP/CLI/REST 都收敛到服务端资产核心，原 Web provider 能力作为迁移来源。
- 2026-06-18: 已根据小团队/单体平台定位弱化人工审核，第一版默认采用轻量选优/状态标记，质量主要通过 prompt、style preset、参考图和后续 best-of 策略保证。
- 2026-06-18: 已完成 MCP stdio entry：`/app/mcp` 支持 initialize、tools/list、tools/call；真实 smoke 跑通 create task -> get task -> select asset -> get delivery info，MCP 输出使用 generated/selected 语义。
- 2026-06-18: 已完成服务端 OpenAI-compatible provider adapter：支持 `images/generations`、`data[].b64_json`、`data[].url`、PNG 规范化、raw response/cost/parameters 记录；本地 HTTP mock 集成 smoke 跑通 `provider=openai-compatible` 任务到 ready asset。
- 2026-06-18: 已完成 Web managed mode 第一版：设置页可开启服务端托管模式并配置 API URL / workspace / project / campaign / provider；Web 创建服务端 `ImageTask`，轮询 `GET /api/tasks/{id}`，任务卡和详情页展示服务端候选图，详情页可 select/reject 并打开 original / metadata URL；Web 测试、构建、Docker Compose build 和 REST mock smoke 均通过。
- 2026-06-18: 已完成 Quality foundation：使用 `project.metadata_json.quality_profile` 保存项目级 prompt template、style preset、reference image 参数和 generation config；REST/MCP/Web 创建任务可通过 `use_project_quality_profile` 复用配置，模板渲染后的有效 prompt 和配置快照写入 `structured_input_json`；Go/Web 测试、Docker Compose build、REST profile smoke 和 MCP create smoke 均通过。
- 2026-06-18: 已完成 Best-of auto selection 第一版：`selection_mode` 进入统一 `ImageTask` 输入、`structured_input_json` 和 MCP/Web 托管创建路径；Worker 在 `auto` / `best_of` 模式下用 `local_metadata_v1` 本地启发式自动 selected 一张候选；REST smoke 验证 auto 任务恰好 1 张 selected、manual_optional 任务 0 张 selected，自动 selected 可人工 reject 覆盖。
- 2026-06-18: 已完成 Advanced managed input 第一版：REST/MCP/Web 可传 `reference_images` 扩展 descriptor、`mask_image` 和 `generation_config`；Web managed mode 不再拒绝输入图或 mask；mock REST smoke 验证 asset `parameters_json` 保留 reference/mask/generation config 快照。
- 2026-06-18: 已完成 Repair/Reconcile smoke 第一版：新增本地 `vag repair scan/requeue/verify-asset`；Docker smoke 验证模拟 `enqueue_failed` 任务可被 scan 报告、requeue 后由 Worker 处理完成，资产文件存在时 `verify-asset ok=true`，缺失 original 时报告 `missing_file`。
- 2026-06-18: 已完成 Worker retry/backoff 第一版：Worker 会先 promote Redis delayed queue，再消费主队列；provider 瞬时失败会把 attempt 标记 failed 并写入 `retry_after`，任务回到 `queued` 后自动重试；Docker smoke 验证 `mock_failure_mode=transient_once` 任务第 1 次失败、第 2 次成功。
- 2026-06-18: 已完成真实缩略图 slice：Worker/Storage 不再依赖 provider thumbnail bytes，而是基于原图生成 `.webp` 缩略图；Docker smoke 验证 `thumbnail_path` 为 `.../1.webp`、`GET /api/assets/{id}/thumbnail` 返回 `image/webp`，文件头为 `RIFF....WEBP`。
- 2026-06-18: 已完成项目级 API key + Basic Auth slice：`project.metadata_json.access_config` 保存单把项目 key 的 hash/name/preview，REST 支持 `GET/POST /api/workspaces/{workspace_id}/projects/{project_id}/access-config`，API 可选启用实例级 Basic Auth，项目级请求可要求 `X-API-Key` 或 Bearer；Docker smoke 验证未认证 `401`、仅 Basic 访问受保护 project `401`、Basic + project key 可创建任务并读取 asset，CLI `vag` 和 Web managed mode 均已支持透传鉴权。
- 2026-06-18: 已完成 Web scope selector / quick create slice：REST 新增列出/创建 workspace、project、campaign；Web 设置页可同步服务端 scope、用下拉选择当前 workspace/project/campaign，并直接快速新建后自动切换；Docker smoke 验证新建 `ws_scope_smoke -> prj_scope_smoke -> cmp_scope_smoke` 后可直接在新 campaign 下创建任务并产出 asset。
- 2026-06-18: 已完成 Managed input upload / real edit-mask slice：REST 新增 scope 内 `input-files` 上传、元数据和内容读取；Web managed mode 会先上传 reference image / mask，再创建带 `input_file_id` 的服务端任务；Docker smoke 使用本地 HTTP mock 验证 Worker 真实调用 `/images/edits` multipart（`image_count=1`、`mask_count=1`）并完成 `task_dd1a410a094e30f06fc5 -> asset_fb9f0bbe559c4c95aa88` 闭环。
- 2026-06-18: 已完成 README/demo/self-hosting 文档 slice：重建 `README.md` 为仓库入口，补齐 quickstart、mock/Web/MCP demo、自托管最小暴露面和反向代理/TLS 样例；`RUNBOOK.md` 同步补充最小自托管建议；`docker compose config`、`curl -u admin:secret http://127.0.0.1:8081/healthz` 和 `curl http://127.0.0.1:8080` 验证通过。
- 2026-06-18: 已完成 Standalone scope management slice：REST 新增 workspace/project/campaign `PATCH` / `DELETE`，列表返回 `archived`；Web 顶栏新增独立 scope 管理 modal，可 rename/archive/delete 并设为当前 scope；Docker smoke 验证 rename、archive/unarchive、非空 scope 删除报错、campaign 删除后的 `input-files` 目录清理，以及 workspace/project/campaign 最终可完整删除。
- 2026-06-18: 已完成 Remote URL + asset reuse slice：服务端 `CreateTask` 现在可解析 `reference_images[].url`、`reference_images[].asset_id`、`mask_image.url`、`mask_image.asset_id`；remote URL 会被抓取并物化为当前 scope `input-files`，`asset_id` 复用限定在当前 workspace/project；Docker smoke 验证 `GET /remote.png` 被服务端抓取、OpenAI-compatible 请求命中 `/v1/images/edits` 且 `image_count=2`，并成功完成 `task_91237d5d15aa7252bed4 -> asset_9ab0aeca719c6e9a2f66`，最终 `parameters_json.reference_images` 同时保留原始 remote URL、生成的 `input_file_id` 和复用的 `asset_id`。
- 2026-06-18: 已完成 fal provider input reuse slice：服务端新增 `provider=fal` queue + rest storage adapter，统一复用 `resolved_input_files`；Docker smoke 验证 `GET /remote.png`、两次 `POST /rest/storage/upload/initiate`、`POST /queue/openai/gpt-image-2/edit`，并成功完成 `task_0dbae47c6d0459cd8c2c -> asset_96d78f9da6b1fcdb0cca`，最终 asset `parameters_json` 保留 `request_mode=edit`、`endpoint_id=openai/gpt-image-2/edit` 和原始 reference 快照。
- 2026-06-18: 已完成 Best-of pluggable scoring slice：服务端在任务输入和项目级 quality profile 中新增 `best_of_config`，scorer registry 当前支持 `local_metadata_v1` 与 `http_judge_v1`；本地 HTTP judge smoke 验证 `task_9f3b4f5551fbdf5b8e06 -> asset_1837f5fd3e8e6977dcb3`，mock judge 收到 `POST /score` 候选缩略图 data URL，请求与 `review_event.note` 一致记录 `requested_strategy=http_judge_v1`、`applied_strategy=http_judge_v1`。
- 2026-06-18: 已完成 Best-of auto reject slice：服务端在 `best_of_config` 中新增 `auto_reject_non_selected`，自动选优后可事务式写入 `selected + rejected siblings`；本地 smoke 验证 `task_79ee5fdfe639cd532805` 生成 1 张 selected 与 2 张 rejected，并成功将 auto rejected 的 `asset_5d207d1a89b3ba6d6793` 手动重新 select 为 approved/selected。
- 2026-06-18: 已完成 HTTP rate limiting slice：服务端 `api` 进程新增 Redis 固定窗口限流，支持 `RATE_LIMIT_WINDOW_SECONDS`、`RATE_LIMIT_INSTANCE_MAX_REQUESTS`、`RATE_LIMIT_PROJECT_MAX_REQUESTS`；局部/全量 Go 测试、`npm --prefix web run build`、`docker compose config` 通过；Docker smoke 验证独立 `prj_rate_limit_smoke` 下 `POST /tasks` 命中 `201 -> 429`，实例级请求也返回了 `429` 与 `Retry-After`。
- 2026-06-18: 已完成 HTTP / API audit log slice：服务端 `api` 进程会把 `/api/*` 请求写入 `STORAGE_ROOT/audit/http-api/YYYY-MM-DD.jsonl`；局部/全量 Go 测试、`npm --prefix web run build`、`docker compose config` 通过；Docker smoke 验证 `create_task`、`get_task` 与 `404 not_found` 都会留下审计记录，并可通过 `docker compose exec api /app/vag audit list --project prj_audit_smoke --task task_071eb31a8b161a4de0b5` 查询。
- 2026-06-18: 已完成 project multi-key slice：服务端 `project.metadata_json.access_config` 已支持兼容视图 + `api_keys` 列表；全量 `go test ./...`、`npm --prefix web run build`、`docker compose config` 通过；Docker smoke 验证 `prj_multi_key_1781784728` 同时接受 `default` 与 `rollout` 两把 key，旧 key disable/delete 后 `rollout` 仍能读取 `task_fc9e1275b4dcb665e766`，并在审计里记录 `actor=rollout`。
- 2026-06-19: 已将第二阶段 P0 visibility 需求拆成可由 `/goal` 执行的 CSV 工单：`issues/next-phase-p0-visibility.csv`；范围限定为 Web 服务端资产同步、最小资产库、Scope Dashboard、source/session metadata 标准和整体回归检查。
- 2026-06-19: 已完成第二阶段 P0 visibility：通过 MCP 创建 `task_8df69831d9ae5c4aa92a -> asset_01cc40da9ee74c9f5368`，REST `list assets` 返回 `metadata_json.source=mcp`、`session_id=p0vis_open_1781800411`、`batch_id=p0vis_open_1781800411_batch` 和 delivery URLs；浏览器刷新后 Web 服务端资产库仍显示该 MCP asset，并可执行 reject/select，REST 最终状态回到 `approved`。
- 2026-06-19: 已完成 P1 Storage Governance `P1-STOR-001` 到 `P1-STOR-005`：`npm --prefix web test -- --run` 通过 17 files / 222 tests，`npm --prefix web run build` 通过且仅有既有 chunk warning，Docker `go test ./...` 通过；`GET /api/workspaces/ws_default/projects/prj_xhs_anime/campaigns/cmp_7day_cover/storage-governance` 未带 project key 返回 401，带 smoke key 返回 200，并只返回统计数字/scope id；`vag storage cleanup-preview --limit 5` 返回 `dry_run=true`，候选只包含 `generated_unselected_asset` 和 `rejected_asset`，同时报告 selected/published protected 计数；浏览器 Scope 管理已显示 `storage/original/thumbnail/metadata` 统计。
- 2026-06-19: 已完成 P1 Storage Governance `P1-STOR-006` 到 `P1-STOR-008`：新增 `vag storage cleanup-execute` 受控执行、storage-root 相对路径删除保护、draft/rejected 资产 DB 行事务删除和 CLI audit 记录；Docker fixture `ws_p1stor_smoke_1781852988/prj_p1stor_smoke_1781852988/cmp_p1stor_smoke_1781852988` 生成 3 张 mock 资产，approve 1 张、reject 1 张、保留 1 张 draft，dry-run 返回 2 个候选和 1 个 protected；无 `--execute` 执行被拒绝并写入失败 audit，带 token 执行删除 2 个候选 / 6 个文件并写入成功 audit；后续 list assets 只剩 approved 资产，二次 dry-run 为 0，`storage-integrity` 返回 `ok=true issue_count=0`，浏览器 Scope 管理显示该 workspace 的 post-cleanup 统计。
- 2026-06-19: 已完成 P1 Asset Production Readiness：新增 `AssetListQuery`、metadata 标准化、`project.metadata_json.provider_profile`、provider model override、`vag asset list` 筛选参数和 `vag project provider get/set`；Web 服务端资产库新增 status/provider/source/session/batch/keyword 筛选、limit=24 首屏、加载更多、图片 lazy loading 和折叠详情；新增 `sample-codex-pet-story-task.json` 与 `sample-codex-embedded-architecture-task.json`；`npm --prefix web test -- --run` 通过 17 files / 222 tests，`npm --prefix web run build` 通过且仅有既有 chunk warning，Docker `go test ./...` 通过；REST smoke 验证 `task_a1e1ef5a230207e4bda9 -> asset_b8ddafa3547c75aca91a`，按 `source/session_id/batch_id` 过滤可查回该资产；浏览器验证 Web 资产库首屏 24 张 lazy thumbnail，按 source/session/batch 筛选后仅显示该 P1 smoke asset。
- 2026-06-19: 已完成 P1 Web Performance / Startup：只读 profile 显示用户侧 Chrome `High memory usage` 约 1.1GB，Vite dev server 进程约 132-136 MB RSS，production preview 进程约 103 MB RSS，因此 dev/HMR 有放大但不是唯一来源；本轮未清理 IndexedDB 或资产。已新增 `initStore` in-flight/completed guard、thumbnail backfill 后台队列 120 / 每 session 48 上限、TaskGrid 首屏 60 条 + 加载更多、ServerAssetLibrary 最多保留 120 个已渲染资产、fal/custom 自动恢复 5 个 / 6 小时窗口、Scope 统计 60s 缓存和扫描边界；`App` 懒加载 Agent/Settings/Scope/Detail/Lightbox/Mask，`MarkdownRenderer` 按需加载 streamdown/KaTeX CSS。`npm --prefix web test -- --run` 通过 17 files / 224 tests，`npm --prefix web run build` 通过，主入口包由约 951 KB 降到约 711 KB；浏览器自动化在高内存页面上 `transport closed`，后续若仍复现需单独做 heap snapshot / virtualized list 专项。
- 2026-06-19: 已完成并发与生图性能专项：默认 `WORKER_CONCURRENCY=6`、`OPENAI_COMPATIBLE_MAX_CONCURRENCY=6`；task attempts REST/CLI/Web 可观测，openai-compatible `quality/moderation/output_compression` 透传，新增 `vag benchmark image-generation`。本地 mock 延迟压测：worker=1 32 任务 `mock_delay_ms=250` wall-clock 12.427s，worker=4 同配置 wall-clock 2.979s；worker=6 最大 mock 组 32 任务 / `requested_count=4` / 128 资产 wall-clock 14.239s 且全部完成。真实 provider 经用户确认后执行 c6 小样本：6 任务 4 成功、2 个 120s timeout，worker 内存峰值约 50.60MiB，API 约 26.48MiB，说明瓶颈在 provider 侧而不是本机资源。
- 2026-06-19: 已完成 P1 Provider Throughput & Reliability：默认保留 `WORKER_CONCURRENCY=6`，真实 provider 默认收敛为 `OPENAI_COMPATIBLE_MAX_CONCURRENCY=3`、`FAL_MAX_CONCURRENCY=3`、`PROVIDER_TIMEOUT_SECONDS=300`，openai-compatible 增加 connect/header/total timeout profile；`task_attempt` 新增 `queue_wait_ms/provider_first_byte_ms/provider_total_ms/response_download_ms/store_ms/thumbnail_ms/retry_count/error_stage/response_bytes`；provider profile 增加 `max_n/supports_url_result/preferred_response_format/max_concurrency/timeout_seconds`；`requested_count` 超过 provider `max_n` 时按同 task 拆分 provider 请求；新增 `GET /api/projects/{project_id}/campaigns/{campaign_id}/batch-progress` 和 `vag batch progress`。验证：Docker `go test ./...` 通过，`npm --prefix web test -- --run` 通过 17 files / 224 tests，`npm --prefix web run build` 通过且仅有既有 chunk warning，`docker compose config` 通过；mock benchmark `bench_p1_provider_rel_batch` 3 任务 / 6 资产全部完成，batch progress 返回 `task_count=3`、`succeeded_count=3`、`asset_count=6`、`attempt_count=3`；task `task_6330ef96181ccb074ca5` attempts 可见 `queue_wait_ms/provider_total_ms/store_ms/thumbnail_ms/retry_count`；真实 provider benchmark 未运行，因为仍需费用确认。
- 2026-06-20: 已完成 P1 Web Console Auth & Asset Visibility：新增 Admin login/me/logout、HttpOnly Admin session cookie、`GET /api/admin/assets/recent`、Web Recent Assets 默认视图和资产卡 scope 切换；REST/project-key smoke 创建 `task_04d4615128bca9babecb -> asset_97eb0fc7e168ab0a54a4`，Admin recent 可无 project key 查到该资产，匿名 recent 返回 401，project key list assets 仍可查到同一资产；浏览器验证未登录显示 `unauthorized` 而不是 `0 shown`，登录后 Recent Assets 显示该资产的 workspace/project/campaign、source/session/batch，点击 `Scope` 后 Web settings 切到对应 scope。验证：Docker `go test ./...` 通过，`npm --prefix web test -- --run` 通过 17 files / 224 tests，`npm --prefix web run build` 通过且仅有既有 chunk warning，`docker compose config` 通过。
- 2026-06-20: 已落盘下一阶段 P1 Project Production Context 核心 CSV：`issues/next-phase-p1-project-production-context.csv`。本轮计划收束为必要核心需求，先解决 project 级长期视觉生产上下文，包括角色卡、项目级参考图、Prompt Recipe、Quality Profile 2.0、任务输入展开、最小 Web 面板和萌宠故事 mock 回归；Batch / Story / Scene View、Export Pack、NAS/WebDAV/SMB、Usage Tracking 和 edit lineage 暂缓到核心闭环之后。
- 2026-06-20: 已完成 P1-PCTX-001/002 基线复核与契约确认：只读复核 AGENTS、docs/project、CSV、现有 `quality_profile` / `provider_profile`、asset list、metadata_json 和 input reuse 后，确认本 CSV 是当前主入口；第一版 Project Visual Context 使用 `project.metadata_json.visual_context` 保存角色卡、reference binding 和 prompt recipe，不新增数据库表、迁移或破坏性公共接口。
- 2026-06-20: 已完成 P1-PCTX-003 到 P1-PCTX-006 服务端核心：新增 `GET/POST /api/workspaces/{workspace_id}/projects/{project_id}/visual-context`、`vag project context get/set`、MCP `create_image_task` 的 `character_ids/reference_asset_ids/prompt_recipe_id/use_project_visual_context` 字段，以及 CreateTask 展开逻辑；`go test ./...`、`npm --prefix web test -- --run`、`npm --prefix web run build`、`docker compose config --quiet` 均通过；mock smoke 验证 CLI/REST `task_099adb62c6c7d7cb25cb -> asset_84172190f72d61f701c2` 和 MCP `task_582c7440fd46425545cc -> asset_50d7842428660394d469`，两者均保留 `visual_context_snapshot`。
- 2026-06-21: 已完成 P1-PCTX-007 MCP REST CLI contract and examples：新增 `examples/tasks/project-visual-context-usage.md`、`sample-pet-story-visual-context-scene-002.json`、`scene-003.json`、`rest-create-task.json` 和 JSONL-compatible `mcp-call.json`；JSON parse 检查 6 个示例文件通过；mock smoke 在 `prj_pctx_smoke_1781964012 / cmp_pctx_smoke_1781964012` 下设置 visual context，创建 CLI `task_d1bdf01c13450287d6d2`、REST `task_bf41909b09cdb53c7387`、MCP `task_7efdece0e104a504f613`，batch progress 返回 `task_count=4`、`succeeded_count=4`、`asset_count=8`；默认 project 正确要求 project API key，本轮未读取或打印任何 key；`npm --prefix web test -- --run`、`npm --prefix web run build`、`git diff --check` 均通过。
- 2026-06-22: 已完成 P1-PCTX-008/009 场景补全和最小功能设计，不实现代码、不运行真实 provider、不读取或打印任何 key。新增 `docs/project/stories/slice-037-pctx-web-panel-and-pet-story-scenarios.md`，明确 Web Project Context 面板要覆盖发现/未登录/空态、角色卡维护、资产卡 Mark as reference、Prompt Recipe 维护、Web managed task context selector、错误恢复和 UX smoothness；萌宠故事回归要用 clean project/campaign/session/batch、三角色、style reference、`pet_story_cover` recipe、2-3 个 scene task、batch progress、asset metadata 和 Web 可见性闭环。该记录是需求设计收口；P1-PCTX-008 后续已在 `slice-038` 完成，P1-PCTX-009 后续已在 `slice-039` 完成。
- 2026-06-22: 已完成 P1-PCTX-008 Minimal Web project context panel：新增 Web Project Context lazy modal/header entry，接入 `GET/POST /visual-context`，可显示 loading/empty/unauthorized/error，支持 character、reference binding、prompt recipe 的最小新增/编辑/归档；Server Asset Library asset card 新增 `Reference` 动作，带入 `asset_id` 打开 reference binding 表单，不改变 asset 文件/status；InputBar 托管模式新增 Project Context selector，Web managed task 创建时发送 `character_ids/reference_asset_ids/prompt_recipe_id/use_project_visual_context`，并且托管提交不再依赖前端 OpenAI profile key。验证：focused tests 3 files / 98 tests 通过，`npm --prefix web test -- --run` 17 files / 226 tests 通过，`npm --prefix web run build` 通过且仅有既有 chunk warning，`git diff --check` 通过，`curl -I http://127.0.0.1:4173/` 返回 200；`agent-browser-cli` smoke 确认 production preview root 可见，Project Context modal 可打开并显示明确 `unauthorized / login required`。本轮未运行真实 provider，未读取或打印任何 API key / provider key / secret。P1-PCTX-009 后续已在 `slice-039` 完成。
- 2026-06-22: 已完成 P1-PCTX-009 Pet story core regression and docs：clean project/campaign 为 `prj_pet_story_pctx009_1782094416 / cmp_pet_story_pctx009_1782094416`；style reference task `task_4dfbbd870dbb99f2e9fc` 生成同 project reference asset `asset_b8d5272e4afa0e249e5f`；project visual context 包含 `dog_mochi`、`dog_biscuit`、`cat_orange`、style reference 和 `pet_story_cover` recipe。最终 scene-only session/batch/story 为 `pet_story_pctx009_scene_session_1782094416 / pet_story_pctx009_scene_batch_1782094416 / pet_story_pctx009_scene_story_1782094416`，scene tasks `task_6e6cf178fcf656386d62`、`task_2fca7b06875125b64d01`、`task_108de9bcdaafc384302f` 全部 completed，生成 6 张 assets；batch progress 返回 `task_count=3`、`succeeded_count=3`、`failed_count=0`、`asset_count=6`、`attempt_count=3`；REST/CLI asset list 和 Admin Recent Assets 按 source/session/batch 均查回 6 张 scene assets，覆盖 `scene_001`、`scene_002`、`scene_003`；task/asset 快照保留 `visual_context_snapshot`、character ids、reference asset id、recipe id、story_id、scene_id 和 batch_id。验证：`docker compose ps`、healthz、`docker compose config --quiet`、`npm --prefix web test -- --run`、`npm --prefix web run build`、容器化 `go test ./...` 和 preview HTTP 200 均通过。本轮未运行真实 provider，未读取或打印任何 API key / provider key / secret。
- 2026-06-22: 已生成下一阶段 P1 Batch Story Export Foundation 计划：新增 `docs/project/stories/slice-040-batch-story-export-scenarios.md` 和 `issues/next-phase-p1-batch-story-export-foundation.csv`。任务从 P1-BSE-001 到 P1-BSE-011，覆盖 baseline、summary contract、summary API、MCP filter parity、Web production view、scene asset actions、scene regenerate design/implementation、minimal export manifest、ZIP boundary、NAS Docker guide 和 regression docs。该计划只做批量故事图片资产生产、审看和交付，不做小红书发布、内容日历、通用 DAM、WebDAV/SMB server、多人协作或 AI 视觉质检。
- 2026-06-22: 已完成 P1-BSE-001 Baseline and scope guard：监督只读 explorer 复核现有 REST/CLI/Admin Recent Assets filters、CLI batch progress、delivery URL、Web asset library、storage foundation 和 MCP `list_image_assets` session/batch filter gap；确认 `issues/next-phase-p1-batch-story-export-foundation.csv` 为下一阶段主入口，并同步项目管理文件。下一步是 P1-BSE-002 Story scene summary contract，需要先确认 summary response、scene regenerate 和 manifest 语义。
- 2026-06-22: 已完成 P1-BSE-002 Story scene summary contract：新增 `docs/project/stories/slice-041-batch-story-summary-contract.md`，确认 `batch-summary` 路由、metadata-only grouping、默认 scene task 聚合、setup task 排除、public asset status mapping、scene regenerate metadata 和 manifest relationship。下一步是 P1-BSE-003 Batch story summary API；该实现不需要新增数据库表。
- 2026-06-22: 已完成 P1-BSE-003 Batch story summary API：新增 `docs/project/stories/slice-042-batch-story-summary-api.md`；服务端新增 `BatchStorySummary` domain DTO、Postgres metadata-only 聚合、service 透传、HTTP `GET /api/projects/{project_id}/campaigns/{campaign_id}/batch-summary`、Admin session allowance 和 audit route。接口要求至少一个 `session_id` 或 `batch_id`，支持 `story_id/source/status/include_setup/limit`，默认排除空 `scene_id`、setup/reference/calibration task role 和 `exclude_from_story_summary=true` 并计数；资产状态对外映射 `draft -> generated`、`approved -> selected`，summary 响应只返回公开 URL，不返回本地绝对路径。新增 `/api/assets/{asset_id}/metadata` 返回不含 `local_path` 的 metadata URL 目标。验证：容器化 `gofmt` 和 `go test ./internal/domain ./internal/store ./internal/app ./internal/httpapi` 通过。本轮未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1-BSE-004 MCP list filters alignment：`internal/mcp/server.go` 扩展 `list_image_assets` schema，新增 `source/session_id/batch_id/status/keyword/limit`，并在 tool call 中透传到 `domain.AssetListQuery`；默认 project/campaign scope 不变，未新增 MCP summary tool；MCP semantic output 会移除 `local_path`，避免暴露本地文件路径。新增 `internal/mcp/server_test.go` 覆盖 schema、过滤参数透传、默认 scope、limit 透传和 local_path 脱敏。验证：容器化 `gofmt` 和 `go test ./internal/mcp` 通过。本轮未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1-BSE-005 Web production view read-only：新增 `docs/project/stories/slice-044-web-production-view-read-only.md`；Web 新增懒加载 `ProductionViewModal` 和顶栏入口，`agentImageflowApi` 增加 `batch-summary` types、URL builder 和 `getAgentImageflowBatchStorySummary`，支持 `session_id/batch_id/story_id/source/status/include_setup/limit`；视图展示 counts、stories/scenes、scene status、task/asset counts、selected coverage、thumbnail、download/metadata links、latest task id 和 error 摘要；401/403 显示 unauthorized/login required，刷新保留已有结果。验证：`npm --prefix web test -- --run` 17 files / 226 tests 通过，`npm --prefix web run build` 通过且仅有既有 large chunk warning。本轮未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1-BSE-006 Scene asset actions：新增 `docs/project/stories/slice-045-scene-asset-actions.md`；`ProductionViewModal` 的 scene asset cards 复用现有 `selectAgentImageflowAsset` / `rejectAgentImageflowAsset`，分别调用 `/api/assets/{asset_id}/approve` 与 `/reject`；scene header 增加 `selected asset` / `missing selected` 状态，每个 asset 有固定尺寸 Select/Reject 控件和 per-asset pending/error；401/403 动作失败显示 `unauthorized / login required`；成功后本地更新当前 summary 中的 asset status、scene selected/rejected counts、primary selected asset、story selected scene counts 和顶部 coverage counts，不关闭 modal、不清空 summary。验证：`npm --prefix web test -- --run` 17 files / 226 tests 通过，`npm --prefix web run build` 通过且仅有既有 large chunk warning。本轮未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1-BSE-007 Scene regenerate design：新增 `docs/project/stories/slice-046-scene-regenerate-design.md`；设计确认 regenerate 是创建新 task，而不是 retry 或 mutate source task；输入可用 `source_task_id` 或 scene identity + latest selector，并可传 prompt、negative prompt、prompt recipe、character/reference ids、非敏感 generation config 等 overrides；输出返回新 `task_id/status/regenerated_from_task_id/regenerate_no/copied_visual_context_snapshot/warnings`；metadata 规则覆盖 `structured_input_json.metadata_json`、asset `parameters_json`、batch summary/progress/asset list/manifest 可读性；P1-BSE-008 验收标准已明确。本轮只改文档和 CSV，未改业务代码，未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1-BSE-008 Scene regenerate implementation：新增 `docs/project/stories/slice-047-scene-regenerate-implementation.md`；后端新增 scene regeneration REST action、domain DTO、source task/latest scene task/count store helper、`RegenerateSceneTask` service 和 audit/admin session wiring；Web `ProductionViewModal` 新增 per-scene reason input、Regenerate 按钮、pending/error/success 状态和 selected preserved 提示，成功后保留旧 summary 并刷新。验证：容器化 `go test ./internal/domain ./internal/store ./internal/app ./internal/httpapi ./internal/mcp` 通过，`npm --prefix web test -- --run` 17 files / 227 tests 通过，`git diff --check` 通过。本轮未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1-BSE-009 Minimal export manifest：新增 `docs/project/stories/slice-048-minimal-export-manifest.md`；后端新增 `batch-manifest` REST endpoint 和 app manifest builder，CLI 新增 `vag batch manifest`，Web Production View 新增 selected/all/all+rejected JSON 下载；manifest 复用 summary grouping，输出 counts、tasks、assets、scenes、stories、delivery URLs、metadata URLs、target_path 和 visual_context。验证：容器化 `go test ./internal/domain ./internal/app ./internal/httpapi ./cmd/vag` 通过，`npm --prefix web test -- --run` 17 files / 228 tests 通过，`npm --prefix web run build` 通过且仅有既有 chunk warning，`git diff --check` 通过。本轮未实现 ZIP，未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1-BSE-010 Export pack ZIP boundary：新增 `docs/project/stories/slice-049-export-pack-zip-boundary.md`；确认 P1 第一轮不实现服务端 ZIP，继续用 JSON manifest + NAS/filesystem 访问完成交付。未来 ZIP 仅在明确确认后另开实现，且必须限制小批量 selected assets、包含 original/thumbnail/metadata/manifest，并加入数量/大小/路径/secret 保护。本轮未改业务代码，未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-22: 已完成 P1-BSE-011 NAS Docker access guide and regression docs：新增 `docs/project/stories/slice-050-nas-docker-access-guide-and-regression.md`；RUNBOOK 补充 Docker storage root、NAS bind mount、WebDAV/SMB/Finder 只读访问、Postgres dump + storage root 一致快照、manifest 与文件系统路径、metadata 归属和 anti-DAM 边界；DECISIONS 确认文件系统只负责浏览/复制/备份，DB/metadata/manifest 继续作为资产状态、追踪、审核、visual context 和交付清单事实源。验证：`git diff --check` 通过。本轮未改业务代码，未运行真实 provider，未读取或打印 API key / provider key / secret / cookie / session。
- 2026-06-20: 已根据用户试用反馈新增 P1 Web UX Smoothness 专项 CSV：`issues/next-phase-p1-web-ux-smoothness.csv`。子代理只读审视指出闪烁高概率来自 `settings/scope/filter` 与资产库刷新耦合、scope 分步写入、刷新时清空 assets、筛选请求无 debounce、lazy modal 空 fallback 和局部更新重绘；该专项将作为体验 P0/P1 与 Project Production Context 产品化收口并行推进。
- 2026-06-21: 已完成 P1 Web UX Smoothness 第一批资产库稳定性修复：`ServerAssetLibrary` 改为只订阅 Agent ImageFlow 相关 settings 字段，不再因无关设置变化重算整份 settings；刷新和错误路径保留已有资产并显示刷新状态；scope incomplete 会取消旧请求但不把可见列表误清空；文本筛选使用 300ms debounce，旧请求由 request 序号忽略；资产卡 `Scope` 动作一次性写入 workspace/project/campaign。验证通过：`npm --prefix web test -- --run` 17 files / 224 tests，`npm --prefix web run build` 通过且仅有既有 chunk warning，`curl http://127.0.0.1:4173/` 返回 200，`curl http://localhost:8081/healthz` 返回 ok。本轮未运行真实 provider；浏览器插件因工具元数据错误未完成视觉验收，已记录为剩余风险。
- 2026-06-21: 已完成 P1-UX-004 Atomic scope switching：Settings 托管 scope selector、手动兜底 ID 输入和快速新建 workspace/project 现在只在本地 draft 保留不完整 scope，完整 workspace/project/campaign 后才提交全局 settings；关闭 Settings 时保留上一份完整 scope，避免空 project/campaign 被 `normalizeSettings` 补成默认示例 scope；ScopeManager 设当前 scope 改为只写必要完整字段。验证通过：`npm --prefix web test -- --run` 17 files / 224 tests，`npm --prefix web run build` 通过且仅有既有 chunk warning。本轮未运行真实 provider；最终浏览器 Network/Performance 视觉回归已由 P1-UX-009 收口。
- 2026-06-21: 已完成 P1-UX-007 Stable lazy modal fallback and preload：`App` 的 AgentWorkspace、Detail、Lightbox、Settings、ScopeManager、MaskEditor 懒加载 fallback 不再为 `null`，首次加载会显示稳定 overlay/skeleton；新增 `lazyModules` 统一动态 import / preload，Header Settings/Scope/Agent、TaskGrid/AgentWorkspace 任务卡、InputBar 图片预览/Mask/无配置提交入口会在 hover/focus/pointerdown 时预加载对应 chunk。验证通过：`npm --prefix web test -- --run` 17 files / 224 tests，`npm --prefix web run build` 通过且 lazy chunks 仍拆分，仅有既有 chunk warning，`curl -I http://127.0.0.1:4173/` 返回 200，`git diff --check` 通过。本轮未运行真实 provider；最终视觉回归已由 P1-UX-009 收口。
- 2026-06-21: 已完成 P1-UX-006 Scope dashboard stats background loading：`ScopeManagerModal` 的层级请求和 dashboard stats 请求改用独立 request id；层级列表渲染后延迟 180ms 启动 stats，关闭 modal 或刷新层级时取消 timer 并忽略旧 stats 写回；stats 扫描 asset list 时显式使用 `limit=24`，继续保留 60s 缓存和扫描上限。验证通过：`npm --prefix web test -- --run` 17 files / 224 tests，`npm --prefix web run build` 通过且仅有既有 chunk warning，`git diff --check` 通过，`curl -I http://127.0.0.1:4173/` 返回 200。本轮未运行真实 provider；浏览器 Network/Performance 视觉回归已由 P1-UX-009 收口。
- 2026-06-21: 已完成 P1-UX-008 Task and asset card render containment：`TaskCard` 改为 `React.memo` 并只订阅 `settings.alwaysShowRetryButton`；`TaskGrid` 新增 memo 化 `TaskGridItem`，稳定每卡 click/reuse/edit/delete handlers；`ServerAssetLibrary` 新增 memo 化 `ServerAssetCard`，稳定 select/reject/copy/scope callbacks，metadata/parameters 摘要在卡片内 `useMemo`。验证通过：`npm --prefix web test -- --run` 17 files / 224 tests，`npm --prefix web run build` 通过且仅有既有 chunk warning，`git diff --check` 通过，`curl -I http://127.0.0.1:4173/` 返回 200。本轮未运行真实 provider；浏览器 Profiler/Network 证据已由 P1-UX-009 补充。
- 2026-06-21: 已完成 P1-UX-009 UX smoothness regression and docs：`npm --prefix web test -- --run` 17 files / 224 tests 通过，`npm --prefix web run build` 通过且仅有既有 chunk warning，`git diff --check` 通过，`curl -I http://127.0.0.1:4173/` 返回 200；`agent-browser-cli` 连接本地 `http://127.0.0.1:4173/` 标签页，确认 root 与服务端资产库可见；打开 Settings 后 root 仍可见且 0 个 `/api/*` 资源请求；打开 Scope 管理后 root 仍可见且首轮只观察到 `/api/workspaces`；Recent + 同步后 root/library 仍可见且只观察到 1 个 recent 请求。当前浏览器 Recent Assets 显示明确 `unauthorized`，符合 Admin session/API host 状态问题而非渲染空白。本轮未运行真实 provider，未读取或打印 API key / provider key / secret。
- 2026-06-22: 已完成 P0/P1 Deployment Auth, Scope and Project Console：新增 `issues/next-phase-p0-p1-deployment-auth-scope-project-console.csv`；Web production Nginx 同源代理 `/api/*` 与 `/healthz`，Web 默认 API URL 使用当前 origin，Admin session 可授权 asset thumbnail/original/metadata，图片 delivery 未授权响应不返回浏览器 Basic Auth challenge；新增 Admin runtime status 安全摘要、Current Scope 三级导航、Project Context 首屏摘要、同源 delivery URL resolver 和 429 友好提示；部署文档更新为推荐 `PUBLIC_BASE_URL` 指向 Web/HTTPS origin。本轮默认 mock 验收，不运行真实 provider，不读取或打印 provider key / API key / secret / cookie / session。
